# Auto-Research 睡前自动研究系统 - 执行方案文档

> 测试日期: 2025-12-20
> 环境: Windows + Git Bash + Claude CLI v2.0.73

---

## 方案对比

| 方案 | 方式 | 优点 | 缺点 | 状态 |
|------|------|------|------|------|
| **A: Headless Mode** | `claude -p --allowedTools` | 安全、可控 | 需要 Git Bash | ✅ 已测试通过 |
| B: Auto-Accept | `Shift+Tab` | 简单 | 无此功能 | ❌ 不可用 |
| **C: YOLO Mode** | `--dangerously-skip-permissions` | 完全自动 | 风险较高 | ✅ 已测试通过 |

---

## 方案 A: Headless Mode (推荐)

### 原理
通过 `--allowedTools` 参数预先授权工具，Claude CLI 在 Headless 模式下自动执行，无需手动确认。

### 环境要求
- Claude CLI: v2.0.73 ✅
- Git Bash: D:\Git\bin\bash.exe ✅
- Python: 3.10.10 ✅ (用于 JSON 解析)

### 关键配置
```bash
# Windows 必须设置 Git Bash 路径
export CLAUDE_CODE_GIT_BASH_PATH='D:\Git\bin\bash.exe'

# Headless 模式调用
claude -p "研究任务" \
    --allowedTools "WebSearch,WebFetch,Read,Write,Glob,Grep" \
    --output-format json
```

### 启动方式
```bash
# 打开 Git Bash (不是 PowerShell!)
cd /e/410_VibeCoding/30_prompt-engineering/auto-research
./_shared/research.sh
```

### 目录结构
```
auto-research/
├── _shared/
│   ├── research.sh              # ⭐ Bash 启动脚本 (主用)
│   ├── research.ps1             # PowerShell 备用
│   └── prompts/
│       ├── quick-overview.md    # 快速概览 Prompt
│       └── deep-research.md     # 深度报告 Prompt (KNOW 格式)
├── README.md
├── EXECUTION-GUIDE.md           # 本文档
└── 2025-12-20/                  # 按日期组织
    ├── research-topics.json     # 今日主题
    ├── output/
    │   ├── quick/               # 快速概览输出
    │   └── deep/                # 深度报告输出
    └── logs/
        └── research.log
```

### 预估时间
- 4 个主题 × (快速概览 + 深度报告) = 8 个任务
- 每个任务 ~15-25 分钟 + 等待间隔
- **总计: ~3 小时**

---

## 方案 C: YOLO Mode ✅ 已测试通过

### 原理
使用 `--dangerously-skip-permissions` 绕过所有权限确认，完全无人值守运行。

### 测试结果
```
✅ 基础测试: claude -p "回复两个字" --dangerously-skip-permissions
   结果: 成功，返回 "成功"

✅ 工具调用测试: claude -p "搜索 Gemini" --dangerously-skip-permissions --allowedTools "WebSearch"
   结果: 成功，执行了 WebSearch 并返回结果
   耗时: 37 秒
   费用: $0.21
```

### 命令格式
```bash
# 设置 Git Bash 路径
export CLAUDE_CODE_GIT_BASH_PATH='D:\Git\bin\bash.exe'

# YOLO Mode 调用
claude -p "研究任务" \
    --dangerously-skip-permissions \
    --allowedTools "WebSearch,WebFetch,Read,Write,Glob,Grep" \
    --output-format json
```

---

## 方案 A vs 方案 C 深度对比

### 核心区别

```
┌─────────────────────────────────────────────────────────────────────┐
│                        权限控制机制对比                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  方案 A: Headless Mode                                              │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  claude -p "任务" --allowedTools "WebSearch,Read,..."       │   │
│  │                           │                                  │   │
│  │                           ▼                                  │   │
│  │              ┌─────────────────────┐                        │   │
│  │              │   白名单检查        │                        │   │
│  │              │  (只允许指定工具)   │                        │   │
│  │              └──────────┬──────────┘                        │   │
│  │                         │                                    │   │
│  │         ┌───────────────┼───────────────┐                   │   │
│  │         ▼               ▼               ▼                   │   │
│  │    WebSearch ✅     Read ✅      Bash ❌ (未授权)           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  方案 C: YOLO Mode                                                  │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  claude -p "任务" --dangerously-skip-permissions            │   │
│  │                           │                                  │   │
│  │                           ▼                                  │   │
│  │              ┌─────────────────────┐                        │   │
│  │              │   跳过所有权限检查   │                        │   │
│  │              └──────────┬──────────┘                        │   │
│  │                         │                                    │   │
│  │         ┌───────────────┼───────────────┐                   │   │
│  │         ▼               ▼               ▼                   │   │
│  │    WebSearch ✅     Read ✅      Bash ✅ (全部通过)         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 详细对比表

| 特性 | 方案 A: Headless Mode | 方案 C: YOLO Mode |
|------|----------------------|-------------------|
| **命令参数** | `--allowedTools "工具列表"` | `--dangerously-skip-permissions` |
| **权限机制** | 白名单制：只有明确列出的工具才能使用 | 完全禁用：所有权限检查被跳过 |
| **安全等级** | ⭐⭐⭐ 高 | ⭐ 低 |
| **控制粒度** | 精细控制每个工具 | 全有或全无 |
| **适用场景** | 生产环境、日常使用 | 测试环境、隔离沙箱 |

### 可用工具对比

| 工具 | 方案 A (需显式授权) | 方案 C (自动全部可用) |
|------|---------------------|----------------------|
| `WebSearch` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `WebFetch` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `Read` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `Write` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `Edit` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `Bash` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `Glob` | 需在 allowedTools 中指定 | ✅ 自动可用 |
| `Grep` | 需在 allowedTools 中指定 | ✅ 自动可用 |

### 风险分析

```
┌─────────────────────────────────────────────────────────────────────┐
│                          风险等级对比                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  风险类型              方案 A              方案 C                   │
│  ──────────────────────────────────────────────────────────────    │
│                                                                     │
│  意外文件修改          低 (需授权 Write)   高 (32% 用户遇到过)      │
│  ├── 覆盖重要文件      ❌ 不可能           ⚠️ 可能发生              │
│  └── 删除文件          ❌ 不可能           ⚠️ 可能发生              │
│                                                                     │
│  命令执行风险          低 (需授权 Bash)    高                        │
│  ├── 执行危险命令      ❌ 不可能           ⚠️ rm -rf, format 等     │
│  └── 系统配置修改      ❌ 不可能           ⚠️ 可能发生              │
│                                                                     │
│  数据泄露              低                  中                        │
│  └── 读取敏感文件      取决于授权          ✅ 可读取任何文件         │
│                                                                     │
│  网络操作              取决于授权          高                        │
│  └── 发送数据到外部    需授权 WebFetch     ✅ 无限制                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 实际后果案例

**方案 A 的保护机制：**
```bash
# 只授权只读工具
claude -p "分析项目结构" --allowedTools "Read,Glob,Grep"

# Claude 想执行: rm -rf ./temp
# 结果: ❌ 被阻止 (Bash 未在白名单中)

# Claude 想执行: echo "test" > file.txt
# 结果: ❌ 被阻止 (Write 未在白名单中)
```

**方案 C 的风险场景：**
```bash
# YOLO Mode
claude -p "清理项目中的临时文件" --dangerously-skip-permissions

# Claude 可能执行:
# rm -rf ./temp           ← 可能误删重要目录
# git reset --hard        ← 可能丢失未提交的修改
# 覆盖 .env 文件          ← 可能丢失配置
```

### 选择决策树

```
┌─────────────────────────────────────────────────────────────────────┐
│                          选择决策树                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  你的任务是什么?                                                    │
│       │                                                             │
│       ├── 只读研究 (搜索、阅读)                                     │
│       │       │                                                     │
│       │       ├── 重要项目 → 方案 A + "WebSearch,WebFetch,Read"    │
│       │       └── 测试环境 → 方案 C 可接受                          │
│       │                                                             │
│       ├── 需要写文件                                                │
│       │       │                                                     │
│       │       ├── 生产环境 → 方案 A + "...,Write" (精确控制)       │
│       │       └── 隔离环境 → 方案 C 可接受                          │
│       │                                                             │
│       └── 需要执行命令 (Bash)                                       │
│               │                                                     │
│               ├── 任何重要环境 → ⚠️ 强烈推荐方案 A                  │
│               └── Docker/VM 沙箱 → 方案 C 可接受                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 场景推荐总结

| 场景 | 推荐方案 |
|------|---------|
| 日常使用、重要项目 | **方案 A** (Headless + allowedTools) |
| 纯研究任务 (只读) | **方案 A** 或 **方案 C** |
| 快速测试、一次性任务 | **方案 C** 可接受 |
| Docker/VM 隔离环境 | **方案 C** 可接受 |
| 涉及系统命令执行 | **必须用方案 A** |

---

### 安全注意事项
⚠️ YOLO Mode 会绕过所有权限检查，存在风险：
- 意外文件修改 (32% 用户遇到过)
- 数据丢失 (9% 用户报告)
- 系统损坏

**建议**：
1. 仅用于只读研究任务 (WebSearch, WebFetch, Read)
2. 避免授权 Write, Edit, Bash 等写入工具
3. 或在 Docker/VM 隔离环境中运行

### 推荐使用方式

对于 auto-research 研究任务，推荐配置：
```bash
# 只授权只读工具，降低风险
claude -p "研究任务" \
    --dangerously-skip-permissions \
    --allowedTools "WebSearch,WebFetch,Read,Glob,Grep" \
    --output-format json
```

如果需要输出文件，在脚本层面处理写入：
```bash
result=$(claude -p "研究任务" --dangerously-skip-permissions ...)
echo "$result" > output.md
```

---

## 快速启动指南

### 今晚使用 (Git Bash)

```bash
# 1. 打开 Git Bash

# 2. 进入目录
cd /e/410_VibeCoding/30_prompt-engineering/auto-research

# 3. 运行脚本
./_shared/research.sh

# 4. 睡觉 💤
```

### 第二天早上

```bash
# 查看快速概览
explorer ./2025-12-20/output/quick

# 查看深度报告
explorer ./2025-12-20/output/deep

# 查看执行日志
cat ./2025-12-20/logs/research.log
```

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0.0 | 2025-12-20 | 初始版本，方案 A 和 C 测试通过 |
