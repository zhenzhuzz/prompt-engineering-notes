现在我已经收集了足够的信息,让我开始撰写深度研究报告。我会按照 KNOW 格式要求,创建一份 400-600 行的深度报告。

# Claude Code Skills and Agents 深度研究报告

> **Source**: 官方文档 + 技术博客 + GitHub 社区
> **Research Date**: 2025-12-21
> **Sources Count**: 15+ 权威来源
> **Core Theme**: Claude Code 三层扩展体系：Slash Commands (手动触发) + Skills (条件激活) + Sub-Agents (独立上下文工作者)

---

## One Paragraph Takeaway (一段话精华)

**反直觉洞见**: Claude Code 的扩展系统不是"功能叠加",而是"控制层级"——Slash Commands 是你手动按下的按钮,Skills 是 Claude 自动翻阅的工具书,Sub-Agents 是独立思考的协作者。

大多数人错误地把它们当成"三种写 Prompt 的方法",但真正的价值在于:你在用**系统设计思维**配置 AI 的决策树。Slash Commands 让你像操作 Git 一样控制 Claude(`/commit`、`/pr`),Skills 让 Claude 像查文档一样获得领域知识(自动识别何时用 PDF 技能),Sub-Agents 则像 MapReduce 一样并行处理复杂任务(一个审代码、一个写测试、一个优化性能)——这不是 Prompt Engineering,这是 **Agent Orchestration Architecture**。

**核心公式**:
> (手动控制 × 快速响应) + (自动发现 × 领域专精) + (并行隔离 × 深度推理) = Claude Code 的指数级扩展能力

---

## The Essence (核心精华)

```
┌─────────────────────────────────────────────────────────────────┐
│            Claude Code 扩展体系核心架构                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   User Input                  Claude Main Agent                │
│        │                              │                         │
│        ▼                              │                         │
│   ┌─────────────┐                     │                         │
│   │ /command    │ ─────────────────▶  │                         │
│   └─────────────┘                     │                         │
│   Slash Commands                      │                         │
│   • 用户显式调用                       │                         │
│   • 存储的 Prompt 模板                 │                         │
│   • 快速工作流触发                     │                         │
│                                       │                         │
│   ┌─────────────┐                     │                         │
│   │ SKILL.md    │ ◀─────自动读取──────│                         │
│   └─────────────┘                     │                         │
│   Skills (条件激活)                   │                         │
│   • 模型自动发现                       │                         │
│   • description 触发                  │                         │
│   • 渐进式加载                         │                         │
│                                       │                         │
│                                       ▼                         │
│                        ┌────────────────────────┐               │
│                        │    Task Delegation     │               │
│                        └────────────┬───────────┘               │
│                                     │                           │
│            ┌────────────┬───────────┼───────────┬──────────┐    │
│            ▼            ▼           ▼           ▼          ▼    │
│      ┌─────────┐  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌──────┐ │
│      │ Agent 1 │  │ Agent 2 │ │ Agent 3 │ │ Agent 4 │ │ ...  │ │
│      │         │  │         │ │         │ │         │ │      │ │
│      │ Review  │  │ Test    │ │ Debug   │ │ Analyze │ │      │ │
│      └─────────┘  └─────────┘ └─────────┘ └─────────┘ └──────┘ │
│                                                                 │
│   Sub-Agents (独立上下文)                                        │
│   • 独立系统 Prompt                                              │
│   • 隔离的 Context Window                                        │
│   • 仅返回最终结果                                               │
│   • 可并行执行(最多 10 个)                                       │
│                                                                 │
│   💡 核心洞见: 这不是"三种写 Prompt 的方法",而是分层的控制架构   │
│   - Commands = 直接控制层 (你按按钮)                            │
│   - Skills = 知识层 (Claude 查手册)                             │
│   - Agents = 执行层 (独立工作者)                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 目录

- [1. 背景与定义](#1-背景与定义)
- [2. Slash Commands (斜杠命令)](#2-slash-commands-斜杠命令)
- [3. Skills (技能包)](#3-skills-技能包)
- [4. Sub-Agents (子代理)](#4-sub-agents-子代理)
- [5. 三者对比与选择决策](#5-三者对比与选择决策)
- [6. 实战指南](#6-实战指南)
- [7. 最佳实践](#7-最佳实践)
- [8. Transferable Rules](#8-transferable-rules)
- [9. Key Takeaways](#9-key-takeaways)
- [10. Glossary](#10-glossary)
- [11. Sources](#11-sources)

---

## 1. 背景与定义

### 1.1 是什么

Claude Code 提供了三种扩展机制,让你能够定制 Claude 的行为和能力:

| 扩展机制 | 调用方式 | 核心功能 | 类比 |
|---------|---------|---------|------|
| **Slash Commands** | 用户显式调用 (`/commit`) | 快速执行预定义工作流 | Git 命令 (`git commit`) |
| **Skills** | 模型自动发现 | 让 Claude 获得领域专业知识 | 机器人的工具箱(需要时自动拿出) |
| **Sub-Agents** | 任务委派 (自动/显式) | 独立上下文深度推理 | MapReduce 中的并行 Worker |

### 1.2 解决什么问题

**传统 AI 编程的三大痛点**:

| 痛点 | 传统方案 | Claude Code 方案 |
|------|----------|-----------------|
| **重复性任务繁琐** | 每次手写完整 Prompt | Slash Commands 一键触发 |
| **领域知识需反复输入** | 每次对话重复说明背景 | Skills 自动加载相关知识 |
| **复杂任务污染主对话** | 上下文混乱,Token 浪费 | Sub-Agents 隔离上下文 |

**Before/After 对比**:

```
❌ Before (传统方式):
User: "请用我们公司的品牌规范创建一个 PPT,标题用 Montserrat 字体..."
User: "记得加上公司 Logo,颜色用 #007AFF..."
User: "忘了说,每页要加页脚..."

✅ After (Claude Code 方式):
User: /create-branded-presentation "Q4 业绩报告"
[Skills 自动加载: company-branding-guidelines]
[Sub-Agent 独立工作: pptx-generator]
[返回符合规范的完整 PPT]
```

### 1.3 发展历程

| 时间 | 里程碑 | 说明 |
|------|--------|------|
| 2024 Q3 | Slash Commands 引入 | 基础工作流自动化 |
| 2024 Q4 | Agent Skills 发布 | 模型驱动的知识发现 |
| 2025 Q1 | Sub-Agents GA | 独立上下文并行执行 |
| 2025 Q2 | Plugin 生态启动 | 社区驱动的扩展市场 |

---

## 2. Slash Commands (斜杠命令)

### 2.1 核心概念

**定义**: Slash Commands 是**用户显式调用**的预定义 Prompt 模板,存储在 Markdown 文件中。

**工作原理**:
```
用户输入: /review
    ↓
Claude 读取: ~/.claude/commands/review.md
    ↓
将文件内容注入当前对话
    ↓
执行 Prompt 指令
```

### 2.2 文件结构

**存储位置**:

| 类型 | 路径 | 作用范围 |
|------|------|---------|
| **个人命令** | `~/.claude/commands/` | 所有项目通用 |
| **项目命令** | `.claude/commands/` | 当前项目 |

**文件格式**:

```markdown
# /commit 命令示例
---
name: commit
description: 创建符合规范的 Git commit
---

请执行以下步骤创建 Git commit:

1. 运行 `git diff --staged` 查看变更
2. 分析变更内容,提炼核心业务价值
3. 生成 commit message:
   - 标题: emoji + 中文 (不超过 50 字符)
   - 描述: 详细说明变更内容
   - 末尾: 添加 Claude Code 签名

4. 执行 commit 并显示结果
```

**参数支持**:

```markdown
---
name: test
description: 运行指定文件的测试
---

运行以下文件的测试:
$ARGUMENTS

要求:
- 使用项目配置的测试框架
- 显示详细输出
- 如果失败,分析原因并建议修复
```

调用: `/test src/auth.test.ts`

### 2.3 典型用例

```
┌─────────────────────────────────────────────────────────────────┐
│                  Slash Commands 典型工作流                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Git 工作流:                                                    │
│   /commit      →  分析 staged changes + 生成 commit message     │
│   /pr          →  创建 PR + 生成描述 + 添加标签                  │
│   /branch      →  建议分支名 + 创建分支                          │
│                                                                 │
│   代码质量:                                                      │
│   /review      →  代码审查 + 安全检查 + 性能建议                 │
│   /test        →  运行测试 + 修复失败 + 覆盖率报告               │
│   /lint        →  运行 linter + 自动修复                         │
│                                                                 │
│   项目管理:                                                      │
│   /scaffold    →  生成项目脚手架                                 │
│   /refactor    →  代码重构建议 + 执行                            │
│   /docs        →  生成/更新文档                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.4 实战案例: /commit 命令

**文件**: `.claude/commands/commit.md`

```markdown
---
name: commit
description: 创建标准化的 Git commit
---

# Git Commit 工作流

你是一个 Git 提交专家。请按以下步骤创建 commit:

## Step 1: 查看变更
运行这些命令:
- `git status` - 查看文件状态
- `git diff --staged` - 查看暂存的变更
- `git log -3 --oneline` - 查看最近的 commit 风格

## Step 2: 分析变更
识别:
- 变更类型: feat / fix / docs / refactor / test / chore
- 核心业务价值
- 影响范围

## Step 3: 生成 Commit Message
格式:
```
🚀 feat(模块): 简短描述 (不超过 50 字符)

详细描述变更内容和原因:
- 变更 1
- 变更 2

影响:
- 文件 A
- 文件 B

🤖 Generated with [Claude Code](https://claude.com/claude-code)
Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

## Step 4: 执行 Commit
运行: `git commit -m "$(cat <<'EOF' ... EOF)"`

## Step 5: 确认结果
运行: `git log -1` 显示刚创建的 commit
```

**使用效果**:

```bash
User: /commit
Claude: [自动执行完整工作流]
       ✅ 创建 commit: 🚀 feat(auth): 添加 OAuth 登录支持
       📊 变更: 3 files, +127/-45 lines
```

---

## 3. Skills (技能包)

### 3.1 核心概念

**定义**: Skills 是**模型自动发现**的能力包,通过 `description` 字段匹配用户意图,动态加载相关知识和工具。

**关键特性**:

```
┌─────────────────────────────────────────────────────────────────┐
│                   Skills 自动发现机制                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   User: "帮我生成一个公司品牌的 PPT"                             │
│      │                                                          │
│      ▼                                                          │
│   Claude 检索: ~/.claude/skills/ 和 .claude/skills/             │
│      │                                                          │
│      ▼                                                          │
│   匹配 Skills:                                                   │
│   ┌────────────────────────────────────────────────────────┐   │
│   │ pptx-generator:                                        │   │
│   │   description: "Create PowerPoint presentations.      │   │
│   │                 Use when working with PPTX files      │   │
│   │                 or presentations."                    │   │
│   │   ✅ 匹配关键词: "PPT"                                 │   │
│   └────────────────────────────────────────────────────────┘   │
│                                                                 │
│   ┌────────────────────────────────────────────────────────┐   │
│   │ company-branding:                                      │   │
│   │   description: "Company brand guidelines including    │   │
│   │                 logos, colors, fonts. Use when       │   │
│   │                 creating branded content."            │   │
│   │   ✅ 匹配关键词: "公司品牌"                            │   │
│   └────────────────────────────────────────────────────────┘   │
│      │                                                          │
│      ▼                                                          │
│   自动加载两个 Skills 的完整内容                                 │
│   Claude 现在知道:                                               │
│   • 如何创建 PPTX                                                │
│   • 公司品牌规范 (颜色/字体/Logo)                                │
│      │                                                          │
│      ▼                                                          │
│   生成符合品牌规范的 PPT                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 SKILL.md 文件结构

**最小化示例**:

```markdown
---
name: pdf-processing
description: Extract text and tables from PDF files, fill forms, merge documents. Use when working with PDF files or when the user mentions PDFs, forms, or document extraction.
---

# PDF Processing Skill

You are an expert in PDF manipulation using pypdf and pdfplumber.

## Capabilities
- Text extraction
- Table extraction
- Form filling
- PDF merging

## Requirements
Install required packages:
```bash
pip install pypdf pdfplumber
```

## Examples

### Extract text from PDF
```python
import pdfplumber
with pdfplumber.open('document.pdf') as pdf:
    text = ''.join([page.extract_text() for page in pdf.pages])
```

### Fill PDF form
```python
from pypdf import PdfReader, PdfWriter
...
```
```

**关键要素**:

| 字段 | 要求 | 说明 |
|------|------|------|
| `name` | 必填 | 小写字母+数字+连字符,最多 64 字符 |
| `description` | **关键** | 描述功能 + 触发条件,最多 1024 字符 |
| `allowed-tools` | 可选 | 限制可用工具 (如只读权限) |

**Description 最佳实践**:

```
✅ GOOD:
description: "Analyze Excel spreadsheets, create pivot tables, and generate 
charts. Use when working with Excel files, spreadsheets, or analyzing 
tabular data in .xlsx format."

❌ BAD:
description: "For files"
```

**要点**:
1. **说明功能** ("Analyze Excel spreadsheets")
2. **指定触发** ("Use when working with Excel files")
3. **包含关键词** (".xlsx", "spreadsheets", "tabular data")

### 3.3 渐进式加载 (Progressive Disclosure)

Skills 采用**两阶段加载**优化 Token 使用:

```
阶段 1: 发现阶段 (Minimal Tokens)
Claude 只看: name + description
    ↓
    判断是否相关
    ↓
    ❌ 不相关 → 跳过
    ✅ 相关 → 进入阶段 2

阶段 2: 完整加载
读取 SKILL.md 全文
    +
读取引用的支持文件 (reference.md, examples.md)
    +
读取脚本和模板 (scripts/, templates/)
```

**示例目录结构**:

```
pdf-processing/
├── SKILL.md              # 总入口
├── REFERENCE.md          # 详细 API 文档
├── EXAMPLES.md           # 实战案例
├── scripts/
│   ├── extract.py       # 文本提取脚本
│   └── fill_form.py     # 表单填充脚本
└── templates/
    └── invoice.pdf      # 发票模板
```

**SKILL.md 引用文件**:

```markdown
For detailed API documentation, see [REFERENCE.md](REFERENCE.md).

For real-world examples, see [EXAMPLES.md](EXAMPLES.md).

Extract text using:
```bash
python scripts/extract.py document.pdf
```
```

### 3.4 工具权限控制 (allowed-tools)

**用例**: 只读 Skills,安全敏感场景

```markdown
---
name: code-analyzer
description: Analyze code quality, complexity, and security issues. Use when reviewing code or analyzing existing projects. READ-ONLY - does not modify files.
allowed-tools: Read, Grep, Glob, Bash
---

# Code Analyzer (Read-Only)

This Skill provides safe, non-destructive code analysis.

## What you CAN do:
- Read source files
- Search for patterns
- Run static analysis tools (bash commands)

## What you CANNOT do:
- Write or Edit files
- Execute unsafe commands
```

**工具限制示例**:

| Skill 类型 | allowed-tools | 说明 |
|-----------|---------------|------|
| **只读分析** | `Read, Grep, Glob` | 数据分析、代码审查 |
| **安全脚本** | `Read, Bash` | 运行预定义脚本 |
| **完整权限** | (省略字段) | 继承所有工具 |

### 3.5 Skills 发现与管理

**三种来源**:

```
┌─────────────────────────────────────────────────────────────────┐
│                    Skills 优先级与来源                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   优先级从高到低:                                                │
│                                                                 │
│   1️⃣ Plugin Skills (插件提供)                                   │
│      路径: ~/.claude/plugins/{plugin-name}/skills/              │
│      说明: 通过插件市场安装                                      │
│                                                                 │
│   2️⃣ Project Skills (项目级)                                    │
│      路径: .claude/skills/                                      │
│      说明: 团队共享,通过 Git 同步                                │
│                                                                 │
│   3️⃣ Personal Skills (个人级)                                   │
│      路径: ~/.claude/skills/                                    │
│      说明: 个人跨项目使用                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**查看可用 Skills**:

```bash
# 方式 1: 询问 Claude
User: "What Skills are available?"

# 方式 2: 查看文件系统
ls ~/.claude/skills/
ls .claude/skills/

# 方式 3: 使用插件市场
/plugin marketplace search "pdf"
```

### 3.6 实战案例: 公司品牌规范 Skill

**场景**: 团队需要统一的品牌输出

**文件**: `.claude/skills/company-branding/SKILL.md`

```markdown
---
name: company-branding-guidelines
description: Acme Corp brand guidelines including logo usage, color palette, typography, and document templates. Use when creating presentations, documents, or any branded content. Use when user mentions "brand", "company style", "official template".
---

# Acme Corp Brand Guidelines

## Color Palette

| Color | Hex | Use Case |
|-------|-----|----------|
| **Primary Blue** | `#007AFF` | Headers, CTA buttons |
| **Dark Gray** | `#333333` | Body text |
| **Light Gray** | `#F5F5F5` | Backgrounds |
| **Accent Orange** | `#FF9500` | Highlights, warnings |

## Typography

- **Headings**: Montserrat Bold
- **Body Text**: Inter Regular
- **Code**: JetBrains Mono

## Logo Usage

Logo files located in `assets/logos/`:
- `logo-horizontal.svg` - For headers (min width: 200px)
- `logo-vertical.svg` - For square spaces
- `logo-white.svg` - On dark backgrounds

**Requirements**:
- Minimum padding: 20px around logo
- Never distort aspect ratio
- Never recolor (use provided variants)

## Document Templates

See [templates/](templates/) for:
- PowerPoint template (`presentation.pptx`)
- Word template (`document.docx`)
- Email signature (`signature.html`)

## Example Usage

### Create branded PowerPoint
```python
from pptx import Presentation
prs = Presentation('templates/presentation.pptx')
# Template already has:
# - Correct fonts
# - Brand colors
# - Logo in master slides
```

For more details, see [BRAND_MANUAL.pdf](BRAND_MANUAL.pdf).
```

**使用效果**:

```bash
User: "创建一个 Q4 业绩报告的 PPT"

Claude: [自动加载 company-branding-guidelines Skill]
        ✅ 使用 Montserrat 字体
        ✅ 应用 #007AFF 主色调
        ✅ 插入公司 Logo (正确间距)
        ✅ 基于官方模板生成
```

---

## 4. Sub-Agents (子代理)

### 4.1 核心概念

**定义**: Sub-Agents 是**独立上下文的 AI 工作者**,拥有自己的系统 Prompt、工具权限和对话历史,执行特定任务后仅返回最终结果。

**关键特性**:

```
┌─────────────────────────────────────────────────────────────────┐
│              Sub-Agent 独立上下文架构                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Main Claude (主对话)                                           │
│   ┌───────────────────────────────────────────────────────┐   │
│   │ Context: 用户对话 (100 轮)                            │   │
│   │ Token 使用: 已用 50K/200K                             │   │
│   │                                                       │   │
│   │ User: "优化这个模块的性能"                            │   │
│   │   │                                                   │   │
│   │   ▼                                                   │   │
│   │ 决策: 委派给 performance-optimizer agent               │   │
│   └───────────────────────────────────────────────────────┘   │
│          │                                                     │
│          ▼                                                     │
│   ┌───────────────────────────────────────────────────────┐   │
│   │ Sub-Agent: performance-optimizer                      │   │
│   │ ═════════════════════════════════════                 │   │
│   │ Context: **全新的空白对话**                           │   │
│   │ Token 使用: 0/200K (独立计算)                         │   │
│   │                                                       │   │
│   │ 内部工作:                                              │   │
│   │ 1. 读取模块代码 (Read)                                │   │
│   │ 2. 分析性能瓶颈 (Grep 查找 hot paths)                 │   │
│   │ 3. 运行 profiler (Bash)                               │   │
│   │ 4. 识别优化机会                                        │   │
│   │ 5. 生成优化建议                                        │   │
│   │                                                       │   │
│   │ ❗ 这些细节**不会**污染主对话的 Context                │   │
│   └───────────────────────────────────────────────────────┘   │
│          │                                                     │
│          ▼                                                     │
│   仅返回最终结果:                                              │
│   "发现 3 个性能瓶颈:                                          │
│    1. 数据库 N+1 查询 (优化后提速 5x)                          │
│    2. 未缓存的计算 (建议添加 Redis)                            │
│    3. 阻塞 I/O (改为异步可提速 3x)"                            │
│          │                                                     │
│          ▼                                                     │
│   Main Claude:                                                 │
│   [收到简洁结果,继续对话]                                      │
│   "已优化,测试通过,性能提升 8x"                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**与主对话的对比**:

| 特性 | Main Claude | Sub-Agent |
|------|-------------|-----------|
| **Context 隔离** | 累积所有对话 | 每次全新开始 |
| **Token 共享** | 共用 200K 限制 | 独立 200K 配额 |
| **返回内容** | 完整思考过程 | 仅返回结果 |
| **工具权限** | 全部工具 | 可限制子集 |
| **并行能力** | 顺序执行 | 可并行 (最多 10 个) |

### 4.2 Sub-Agent 文件结构

**存储位置**:

| 类型 | 路径 | 作用范围 |
|------|------|---------|
| **项目 Agent** | `.claude/agents/` | 团队共享 |
| **个人 Agent** | `~/.claude/agents/` | 个人跨项目 |
| **插件 Agent** | `~/.claude/plugins/{name}/agents/` | 插件提供 |

**文件格式**:

```markdown
---
name: code-reviewer
description: Expert code review specialist. Proactively reviews code for quality, security, and maintainability. Use immediately after writing or modifying code.
tools: Read, Grep, Glob, Bash
model: sonnet
permissionMode: default
skills: security-checklist, code-standards
---

# Code Reviewer Agent

You are a senior code reviewer ensuring high standards of code quality and security.

## Your Mission

When invoked, immediately:
1. Run `git diff` to see recent changes
2. Focus ONLY on modified files (not entire codebase)
3. Begin review without asking permission

## Review Checklist

### Critical Issues (Must Fix)
- [ ] No exposed secrets or API keys
- [ ] Input validation implemented
- [ ] SQL injection prevented
- [ ] XSS vulnerabilities addressed

### Warnings (Should Fix)
- [ ] Error handling comprehensive
- [ ] No code duplication
- [ ] Performance considerations addressed
- [ ] Test coverage adequate

### Suggestions (Consider Improving)
- [ ] Function/variable naming clear
- [ ] Code comments helpful (not obvious)
- [ ] Logging appropriate

## Output Format

Organize feedback as:

**Critical Issues** (must fix before merge)
- Issue 1: [file:line] Description + Fix suggestion

**Warnings** (should address)
- Warning 1: [file:line] Description + Recommendation

**Suggestions** (nice to have)
- Suggestion 1: Description

**Summary**
- Files reviewed: N
- Critical issues: N
- Recommendation: [APPROVE / REQUEST CHANGES]
```

**配置字段详解**:

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `name` | Agent 标识符 | 小写+连字符 |
| `description` | **触发条件** | 描述何时使用 + 关键词 |
| `tools` | 可用工具列表 | 省略=继承全部 |
| `model` | 使用的模型 | `sonnet` / `opus` / `haiku` / `inherit` |
| `permissionMode` | 权限模式 | `default` / `acceptEdits` / `bypassPermissions` / `plan` |
| `skills` | 自动加载的 Skills | 逗号分隔的 Skill 名称 |

### 4.3 内置 Sub-Agents

Claude Code 提供三个内置 Agent:

```
┌─────────────────────────────────────────────────────────────────┐
│                    内置 Sub-Agents                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   1️⃣ General-Purpose Agent (通用代理)                           │
│   ┌──────────────────────────────────────────────────────────┐  │
│   │ Model: Sonnet (强推理能力)                              │  │
│   │ Tools: 全部工具                                          │  │
│   │ 用途: 复杂多步骤任务,需要探索+修改                       │  │
│   │                                                          │  │
│   │ 使用场景:                                                 │  │
│   │ • 需要读取多个文件并分析关系                              │  │
│   │ • 边探索边修改代码                                        │  │
│   │ • 需要尝试多种策略才能完成任务                            │  │
│   └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│   2️⃣ Explore Agent (探索代理)                                   │
│   ┌──────────────────────────────────────────────────────────┐  │
│   │ Model: Haiku (快速响应)                                  │  │
│   │ Tools: Read, Grep, Glob, Bash (只读命令)                 │  │
│   │ 用途: 快速搜索代码库,**严格只读**                        │  │
│   │                                                          │  │
│   │ Thoroughness 级别:                                        │  │
│   │ • quick      - 基础搜索,最小化探索                       │  │
│   │ • medium     - 平衡速度和彻底性                           │  │
│   │ • very thorough - 全面分析多个位置和命名约定              │  │
│   │                                                          │  │
│   │ 使用场景:                                                 │  │
│   │ • "这个函数在哪里被调用?"                                 │  │
│   │ • "找出所有 API 端点"                                     │  │
│   │ • "搜索错误处理模式"                                      │  │
│   └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│   3️⃣ Plan Agent (规划代理)                                      │
│   ┌──────────────────────────────────────────────────────────┐  │
│   │ Model: Sonnet                                            │  │
│   │ Tools: Read, Glob, Grep, Bash (探索)                     │  │
│   │ 用途: Plan Mode 中的代码库研究                            │  │
│   │                                                          │  │
│   │ 自动触发:                                                 │  │
│   │ • 当 Claude 进入 Plan Mode                               │  │
│   │ • 需要探索代码库以制定计划                                │  │
│   │                                                          │  │
│   │ 限制:                                                     │  │
│   │ • 防止无限嵌套 (Sub-Agent 不能调用 Sub-Agent)            │  │
│   └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 调用方式

**方式 1: 自动委派 (推荐)**

在 `description` 中添加触发关键词:

```markdown
---
name: test-runner
description: Test automation expert. Use PROACTIVELY when user mentions "test", "failing tests", "coverage", or after code changes. MUST BE USED for test-related tasks.
---
```

**触发示例**:

```bash
User: "我改了 auth.ts,跑一下测试"
Claude: [自动调用 test-runner agent]
```

**方式 2: 显式调用**

```bash
User: "Use the code-reviewer subagent to check my recent changes"
User: "Have the debugger subagent investigate this error"
User: "Ask the test-runner agent to fix failing tests"
```

### 4.5 并行执行 (Parallelization)

**限制**: 最多同时运行 **10 个** Sub-Agents

**示例场景**:

```bash
User: "全面审查这个 PR:
       1. code-reviewer 检查代码质量
       2. security-scanner 扫描安全漏洞
       3. test-runner 运行所有测试
       4. performance-analyzer 分析性能
       5. doc-generator 检查文档完整性"

Claude: [并行启动 5 个 Sub-Agents]
        ⏱️ 总耗时: 约 2 分钟 (串行需要 10 分钟)
```

**架构**:

```
Main Claude
    ├─▶ [code-reviewer]     ─────┐
    ├─▶ [security-scanner]  ─────┤
    ├─▶ [test-runner]       ─────┼─▶ 并行执行 (2 分钟)
    ├─▶ [performance]       ─────┤
    └─▶ [doc-generator]     ─────┘
              │
              ▼
    汇总 5 个 Agent 的结果
              │
              ▼
    综合报告: "发现 3 个关键问题..."
```

### 4.6 可恢复 Sub-Agents (Resumable Agents)

**功能**: Sub-Agent 可以恢复之前的对话,保留完整上下文

**工作原理**:

```
第一次调用:
User: "Use code-analyzer to review the auth module"
    ↓
Agent ID: "abc123" 生成
对话历史保存: agent-abc123.jsonl
    ↓
返回结果 + Agent ID

恢复调用:
User: "Resume agent abc123 and also analyze the authorization logic"
    ↓
读取 agent-abc123.jsonl (完整上下文)
    ↓
继续分析 (知道之前的发现)
```

**使用场景**:

| 场景 | 说明 |
|------|------|
| **大型代码库审查** | 分多次会话逐步分析 |
| **迭代式优化** | 逐步改进,不丢失上下文 |
| **多步相关工作流** | 后续步骤依赖前面的发现 |

### 4.7 实战案例: 多 Agent 并行 Code Review

**场景**: PR 全面审查

**定义 3 个 Agents**:

**1. code-quality-reviewer.md**:

```markdown
---
name: code-quality-reviewer
description: Review code structure, naming, and maintainability. Use for code quality checks.
tools: Read, Grep, Glob
model: sonnet
---

Focus on:
- Function/variable naming
- Code duplication
- Function length and complexity
- Code organization
```

**2. security-scanner.md**:

```markdown
---
name: security-scanner
description: Security vulnerability scanner. Use for security audits.
tools: Read, Grep, Bash
skills: owasp-top10, security-checklist
---

Check for:
- SQL injection risks
- XSS vulnerabilities
- Exposed secrets
- Authentication issues
```

**3. performance-analyzer.md**:

```markdown
---
name: performance-analyzer
description: Performance optimization expert. Use for performance reviews.
tools: Read, Grep, Bash
---

Analyze:
- O(n²) algorithms
- Unnecessary loops
- Database N+1 queries
- Blocking I/O
```

**使用**:

```bash
User: "全面审查 PR #123"

Claude: [并行调用 3 个 Agents]

结果:
┌─────────────────────────────────────────────────────────┐
│ Code Quality (code-quality-reviewer)                    │
│ ✅ Overall structure good                               │
│ ⚠️  3 functions exceed 50 lines                         │
│ ⚠️  Variable name `temp` unclear in auth.ts:42          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Security (security-scanner)                             │
│ ❌ CRITICAL: API key exposed in config.ts:15            │
│ ⚠️  SQL query string concatenation in db.ts:89          │
│ ✅ No XSS vulnerabilities found                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Performance (performance-analyzer)                      │
│ ⚠️  N+1 query in getUserOrders() - add JOIN             │
│ ⚠️  Blocking file read in handler.ts:34 - use async     │
│ ✅ No O(n²) algorithms detected                         │
└─────────────────────────────────────────────────────────┘

Recommendation: REQUEST CHANGES
Priority: Fix critical security issue first
```

---

## 5. 三者对比与选择决策

### 5.1 核心对比表

| 维度 | Slash Commands | Skills | Sub-Agents |
|------|----------------|--------|------------|
| **调用方式** | 用户显式 (`/cmd`) | 模型自动发现 | 任务委派 (自动/显式) |
| **触发机制** | 输入命令 | 匹配 description | 检测到复杂任务 |
| **Token 使用** | 注入主对话 | 注入主对话 | **独立计算** |
| **上下文隔离** | ❌ 共享上下文 | ❌ 共享上下文 | ✅ **隔离上下文** |
| **并行能力** | ❌ 顺序执行 | ❌ 顺序加载 | ✅ 最多 10 个并行 |
| **适用场景** | 快速工作流 | 领域知识 | 复杂任务 |
| **典型用例** | Git 操作、测试 | PDF 处理、品牌规范 | 代码审查、深度分析 |

### 5.2 决策树

```
┌─────────────────────────────────────────────────────────────────┐
│                如何选择扩展机制?                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Start: 我需要...                                              │
│      │                                                          │
│      ├─❓ 一个快捷键触发固定流程?                                │
│      │   ✅ Slash Commands                                      │
│      │   例: /commit, /test, /deploy                           │
│      │                                                          │
│      ├─❓ 让 Claude 在特定领域更专业?                            │
│      │   ✅ Skills                                              │
│      │   例: PDF 处理、Excel 分析、公司品牌规范                 │
│      │                                                          │
│      ├─❓ 执行复杂任务但不污染主对话?                            │
│      │   ✅ Sub-Agents                                          │
│      │   例: 代码审查、性能分析、安全扫描                        │
│      │                                                          │
│      └─❓ 多个独立任务需要并行执行?                              │
│          ✅ Sub-Agents (并行)                                    │
│          例: 同时审查 5 个方面 (质量+安全+性能+文档+测试)        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 Daniel Miessler 的三层架构

**核心洞见**: 三种机制不是平等的替代品,而是**嵌套的层级结构**:

```
┌─────────────────────────────────────────────────────────────────┐
│                  三层架构模型                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Skills = Domain Containers (领域容器)                         │
│   ┌───────────────────────────────────────────────────────┐   │
│   │  ~/.claude/skills/blogging/                           │   │
│   │  ├── SKILL.md (路由逻辑)                              │   │
│   │  └── workflows/                                       │   │
│   │      ├── write.md         ◀─ Commands (嵌套任务)     │   │
│   │      ├── publish.md                                   │   │
│   │      └── seo-optimize.md                              │   │
│   └───────────────────────────────────────────────────────┘   │
│                                                                 │
│   Agents = Parallel Workers (独立于 Skill 之外)                │
│   ┌───────────────────────────────────────────────────────┐   │
│   │  ~/.claude/agents/                                    │   │
│   │  ├── code-reviewer.md  ◀─ 可以调用 Skills            │   │
│   │  ├── test-runner.md                                   │   │
│   │  └── debugger.md                                      │   │
│   └───────────────────────────────────────────────────────┘   │
│                                                                 │
│   调用链示例:                                                   │
│   User → /review (Command)                                      │
│       → 触发 code-reviewer (Agent)                              │
│       → Agent 调用 security-checklist (Skill)                   │
│       → Skill 内部执行 workflows/owasp-scan.md (Command)        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**关键规则**:
1. **Skills 组织领域** - 一个 Skill = 一个业务领域 (如 "blogging")
2. **Commands 嵌套任务** - 放在 Skill 的 `workflows/` 目录
3. **Agents 独立存在** - 不嵌套在 Skill 内,但可以调用 Skills

### 5.4 实战选择案例

| 需求 | 选择 | 理由 |
|------|------|------|
| "每次 commit 都要写规范的 message" | `/commit` Command | 固定流程,显式触发 |
| "处理 PDF 时需要专业知识" | PDF-processing Skill | 自动发现,领域知识 |
| "审查代码但不污染对话" | code-reviewer Agent | 隔离上下文,深度分析 |
| "同时审查质量+安全+性能" | 3 个并行 Agents | 并行执行,各司其职 |
| "创建公司品牌的 PPT" | company-branding Skill | 自动加载规范 |
| "快速运行测试" | `/test` Command | 一键触发 |
| "大型重构需要多步规划" | refactor-planner Agent | 复杂推理,保持焦点 |

---

## 6. 实战指南

### 6.1 环境准备

**安装 Claude Code**:

```bash
# macOS
brew install anthropic/claude/claude-code

# 其他平台参考官方文档
https://code.claude.com/docs/en/getting-started
```

**配置文件位置**:

```
~/.claude/
├── commands/          # 个人 Slash Commands
├── skills/           # 个人 Skills
├── agents/           # 个人 Sub-Agents
└── settings.json     # 全局配置

{project}/.claude/
├── commands/          # 项目 Slash Commands
├── skills/           # 项目 Skills (Git 同步)
├── agents/           # 项目 Sub-Agents
└── CLAUDE.md         # 项目特定指令
```

### 6.2 快速开始: 创建第一个 Slash Command

**步骤 1**: 创建文件

```bash
mkdir -p ~/.claude/commands
cat > ~/.claude/commands/hello.md <<'EOF'
---
name: hello
description: Say hello with a fun fact
---

Please greet me and share a random interesting fact about AI or programming.
EOF
```

**步骤 2**: 使用命令

```bash
$ claude

> /hello
Hello! Here's a fun fact: The first computer bug was an actual moth that 
got stuck in Harvard's Mark II computer in 1947. Grace Hopper found it 
and taped it in her log book!
```

### 6.3 快速开始: 创建第一个 Skill

**步骤 1**: 创建 Skill 目录

```bash
mkdir -p ~/.claude/skills/greeting-expert
```

**步骤 2**: 创建 SKILL.md

```bash
cat > ~/.claude/skills/greeting-expert/SKILL.md <<'EOF'
---
name: greeting-expert
description: Expert in greetings and salutations across cultures. Use when user asks about greetings, says hello, or mentions cultural communication.
---

# Greeting Expert

You are an expert in greetings from around the world.

## Knowledge Base

- **English**: "Hello" (formal), "Hi" (casual), "Hey" (very casual)
- **Spanish**: "Hola", "Buenos días" (morning), "Buenas tardes" (afternoon)
- **Japanese**: "こんにちは" (Konnichiwa), "おはよう" (Ohayou - morning)
- **French**: "Bonjour", "Salut" (casual)

## When to use which greeting

- Business context: Use formal greetings
- Friends: Casual is fine
- Time-specific: Use morning/afternoon greetings when appropriate

## Response format

When user asks about greetings, provide:
1. The greeting in target language
2. Pronunciation guide
3. Cultural context (when to use it)
4. Level of formality
EOF
```

**步骤 3**: 测试 Skill

```bash
> Tell me how to greet someone in Spanish

[greeting-expert Skill 自动加载]

In Spanish, you can greet someone with:

1. **"Hola"** (OH-lah)
   - Universal greeting, works any time
   - Formality: Neutral

2. **"Buenos días"** (BWEH-nos DEE-ahs)
   - Use before noon
   - Formality: Polite/formal
   - Literally: "Good days"

Cultural context: In Spanish-speaking cultures, greetings are important 
for building rapport. It's common to greet everyone when entering a room.
```

### 6.4 快速开始: 创建第一个 Sub-Agent

**步骤 1**: 使用 Claude 生成 Agent

```bash
> Help me create a subagent that reviews my git commits for clarity

I'll create a commit-reviewer subagent for you.

[Claude 生成文件: ~/.claude/agents/commit-reviewer.md]

Done! Your commit-reviewer agent is ready. It will:
- Check commit messages for clarity
- Ensure they follow best practices
- Suggest improvements

Try it with: "Use commit-reviewer to check my last commit"
```

**步骤 2**: 使用 Agent

```bash
> Use commit-reviewer to check my last commit

[commit-reviewer agent 启动独立上下文]

Reviewing your last commit:
```
fix bug
```

Issues found:
❌ Too vague - which bug?
❌ No description of what was fixed
❌ Missing emoji prefix (optional but helpful)

Suggested rewrite:
```
🐛 fix(auth): prevent null pointer in token validation

- Added null check before accessing user.token
- Fixes crash when logged-out users hit /api/profile
- Added test case for this scenario
```

Recommendation: Rewrite before pushing
```

### 6.5 使用插件市场

**安装官方 Skills**:

```bash
# 浏览市场
/plugin marketplace

# 安装文档处理 Skills
/plugin marketplace add anthropics/skills
/plugin install document-skills@anthropic-agent-skills

# 现在可用:
# - docx (Word 文档)
# - pdf (PDF 处理)
# - pptx (PowerPoint)
# - xlsx (Excel)
```

**查找社区 Skills**:

- **SkillsMP**: https://skillsmp.com/ (25,000+ skills)
- **GitHub**: https://github.com/topics/claude-skills
- **Awesome Claude Code**: https://github.com/hesreallyhim/awesome-claude-code

---

## 7. 最佳实践

### 7.1 Skills 编写最佳实践

#### 规则 #1: Description 决定成败

**要点**: `description` 字段是 Skill 被发现的**唯一依据**

```
✅ GOOD Description:
description: "Analyze Excel spreadsheets, create pivot tables, and generate 
charts. Use when working with Excel files, spreadsheets, .xlsx files, or 
analyzing tabular data."

关键元素:
1. 功能: "Analyze... create... generate..."
2. 触发: "Use when working with..."
3. 关键词: "Excel", ".xlsx", "spreadsheets", "tabular"

❌ BAD Description:
description: "For files"

问题:
- 太泛化,无法匹配具体场景
- 缺少触发条件
- 没有关键词
```

**技巧**: 包含用户可能使用的**同义词**

```markdown
description: "Create and edit PowerPoint presentations (pptx files). 
Use when user mentions: presentations, slides, PowerPoint, PPT, PPTX, 
deck, pitch deck, slide deck."
```

#### 规则 #2: 保持 Skill 专注

```
✅ GOOD (专注):
pdf-form-filler/     - 只处理 PDF 表单填充
pdf-text-extractor/  - 只提取 PDF 文本
pdf-merger/          - 只合并 PDF 文件

❌ BAD (过于宽泛):
document-processor/  - 处理所有文档类型
    → 触发条件不清晰
    → Claude 难以决定何时使用
```

#### 规则 #3: 使用渐进式加载

**结构**:

```
complex-skill/
├── SKILL.md          # 简洁入口 (200 行)
├── REFERENCE.md      # 详细 API (1000 行)
├── EXAMPLES.md       # 实战案例 (500 行)
└── scripts/
    └── helper.py
```

**SKILL.md**:

```markdown
# Complex Skill

Quick guide for common tasks.

## Basic Usage
[简短示例]

## Advanced
For advanced usage, see [REFERENCE.md](REFERENCE.md).
For real-world examples, see [EXAMPLES.md](EXAMPLES.md).
```

**好处**:
- 初次加载只读取 SKILL.md (节省 Token)
- 需要时才加载详细文档
- 保持主对话清爽

### 7.2 Sub-Agents 编写最佳实践

#### 规则 #1: 明确单一职责

```
✅ GOOD:
code-quality-reviewer  - 只检查代码质量
security-scanner       - 只扫描安全漏洞
performance-analyzer   - 只分析性能

❌ BAD:
code-checker           - 质量+安全+性能 (职责不清)
```

#### 规则 #2: 限制工具权限

```markdown
# ✅ GOOD: 只读 Agent
---
name: code-analyzer
tools: Read, Grep, Glob, Bash
---

This agent is READ-ONLY. It:
- Reads files
- Searches code
- Runs read-only commands (git log, ls, etc.)

It CANNOT:
- Write files
- Edit code
- Execute destructive commands
```

**为什么**:
- 安全性 - 只读 Agent 不会意外修改代码
- 聚焦 - 限制工具让 Agent 专注分析

#### 规则 #3: 添加"Proactive"触发词

```markdown
---
name: test-runner
description: Test automation expert. Use PROACTIVELY when user mentions 
"test", "failing tests", "coverage". MUST BE USED after code changes.
---
```

**关键词**:
- `PROACTIVELY` - 鼓励自动调用
- `MUST BE USED` - 强制触发
- `immediately` - 立即使用

### 7.3 Slash Commands 编写最佳实践

#### 规则 #1: 命令名要简短易记

```
✅ GOOD:
/commit   - 创建 commit
/test     - 运行测试
/review   - 代码审查

❌ BAD:
/create-git-commit-with-message
/run-all-tests-and-show-coverage
```

#### 规则 #2: 支持参数

```markdown
---
name: deploy
description: Deploy to specified environment
---

Deploy to environment: $ARGUMENTS

If no environment specified, default to staging.

Steps:
1. Run tests
2. Build production bundle
3. Deploy to $ARGUMENTS environment
4. Run smoke tests
```

**使用**:

```bash
/deploy production   # 部署到生产
/deploy             # 部署到 staging (默认)
```

#### 规则 #3: 提供清晰的步骤

```markdown
# ❌ BAD (模糊)
Please commit the changes.

# ✅ GOOD (清晰)
## Step 1: Review Changes
Run `git diff --staged`

## Step 2: Generate Message
Create commit message with:
- Summary (50 chars max)
- Detailed description
- File list

## Step 3: Execute Commit
Run: `git commit -m "..."`

## Step 4: Confirm
Show: `git log -1`
```

### 7.4 团队协作最佳实践

#### 规则 #1: 项目 Skills 纳入版本控制

```bash
# ✅ GOOD
.claude/
├── skills/
│   ├── company-branding/
│   │   └── SKILL.md
│   └── api-standards/
│       └── SKILL.md
└── agents/
    ├── code-reviewer.md
    └── test-runner.md

# Git commit
git add .claude/
git commit -m "Add team-wide Skills and Agents"
git push

# 团队成员 git pull 后自动获取
```

#### 规则 #2: 使用 CLAUDE.md 定义团队规范

```markdown
# .claude/CLAUDE.md

# Team Coding Standards

## Git Workflow
- Always use `/commit` command for commits
- PR descriptions: Use `/pr` command
- Branch naming: `type/description-YYYY-MM-DD`

## Code Review
- Automated: code-reviewer agent runs on all PRs
- Manual: Focus on business logic (agent handles style)

## Testing
- Run `/test` before committing
- Minimum coverage: 80%

## Available Skills
- `company-branding` - Use for all customer-facing content
- `api-standards` - Use when designing APIs
```

#### 规则 #3: 文档化 Agent 的预期行为

```markdown
# agents/code-reviewer.md

---
name: code-reviewer
description: [...]
---

# Code Reviewer Agent

## Expected Behavior

This agent will:
✅ Check code quality, security, and performance
✅ Provide actionable feedback
✅ Categorize issues by priority

This agent will NOT:
❌ Fix issues automatically (only suggest)
❌ Review entire codebase (only recent changes)
❌ Block PRs (final decision is human's)

## Team Agreement

- Run on all PRs before requesting human review
- Address all "Critical" issues before merge
- "Warnings" and "Suggestions" are optional
```

---

## 8. Transferable Rules

### Rule 1: Description 驱动发现

**The Pattern**:
```
❌ 错误模式: 写完 Skill/Agent 后才想 description
✅ 正确模式: 先写 description,再写内容
```

**Why it works**:
description 是触发机制的核心。如果你先写内容,很可能写出"什么都能做"的泛化 Skill,导致 Claude 无法判断何时使用。先写 description 强迫你明确:
1. 这个 Skill/Agent 的**单一职责**是什么
2. 用户会用什么**关键词**触发
3. 什么情况下**不应该**触发

**How to apply**:

创建 Skill 时,从这个模板开始:

```markdown
---
description: "[功能描述]. Use when [触发条件]. Do NOT use when [排除条件]."
---
```

填空练习:
- 功能描述: "Analyze Excel data and create charts"
- 触发条件: "user mentions Excel, .xlsx files, or spreadsheet analysis"
- 排除条件: "user only wants to read CSV (use csv-reader instead)"

---

### Rule 2: 隔离污染源

**The Pattern**:
```
❌ 错误模式: 复杂任务在主对话中执行
✅ 正确模式: 用 Sub-Agent 隔离探索过程
```

**Why it works**:
主对话的 Context Window 是**宝贵资源**。如果用主对话执行"尝试 10 种方案最终找到正确答案"的任务,你的 Context 会被无用的中间步骤填满。Sub-Agent 隔离后:
- 中间探索过程消耗 Agent 自己的 Token
- 主对话只收到最终结果 (干净简洁)
- 可以并行执行多个探索任务

**How to apply**:

判断是否需要 Sub-Agent 的标准:

```
需要 Sub-Agent 的任务 (隔离):
- "找出性能瓶颈" (需要尝试多种 profiling 方法)
- "审查代码" (需要检查数十个文件)
- "调试错误" (需要试探多个假设)

直接在主对话的任务 (不隔离):
- "运行测试" (一步操作,结果明确)
- "提交 commit" (标准流程)
- "解释这段代码" (单次回答)
```

**对比**:

```
❌ 主对话污染:
User: "优化 auth.ts 性能"
Claude: 让我试试 profiling...
        [10 轮尝试各种方法]
        [Context 已用 50K tokens]
Claude: 找到了,是这里的 O(n²) 循环

✅ Sub-Agent 隔离:
User: "优化 auth.ts 性能"
Claude: [调用 performance-analyzer agent]
        [Agent 独立探索,10K tokens]
Claude: [收到结果] 发现 O(n²) 循环,已优化
        [主对话只增加 2K tokens]
```

---

### Rule 3: 分层而非平铺

**The Pattern**:
```
❌ 错误模式: Skills, Commands, Agents 平铺在各自目录
✅ 正确模式: 按 Domain 组织,Skills 包含 Commands
```

**Why it works**:
参考 Daniel Miessler 的三层架构:平铺会导致:
- 重复代码 (多个 Commands 需要相同的背景知识)
- 难以维护 (修改一个领域需要改多个文件)
- 难以分享 (无法打包完整领域)

分层组织后:
- Skill = Domain Container (包含该领域的所有知识)
- Commands = 嵌套任务 (复用 Skill 的背景知识)
- Agents = 独立工作者 (可以调用多个 Skills)

**How to apply**:

**重构前**:

```
~/.claude/
├── commands/
│   ├── write-blog.md
│   ├── publish-blog.md
│   └── seo-optimize.md
└── skills/
    └── blogging-guidelines/
```

**重构后**:

```
~/.claude/
├── skills/
│   └── blogging/                    ← Domain Container
│       ├── SKILL.md                 ← 路由逻辑 + 共享知识
│       └── workflows/
│           ├── write.md             ← Commands (嵌套)
│           ├── publish.md
│           └── seo-optimize.md
└── agents/
    └── content-writer.md            ← 可以调用 blogging Skill
```

**SKILL.md (路由)**:

```markdown
---
name: blogging
description: Blog writing and publishing workflows. Use for all blog-related tasks.
---

# Blogging Domain

## Shared Knowledge
- Target audience: developers
- Tone: casual but professional
- Length: 1000-2000 words

## Available Workflows

For specific tasks, check:
- [Write new blog](workflows/write.md)
- [Publish blog](workflows/publish.md)
- [SEO optimize](workflows/seo-optimize.md)
```

**好处**:
1. 共享知识 (所有 workflows 知道 "Target audience: developers")
2. 打包分享 (整个 `blogging/` 目录可分享)
3. 维护简单 (修改受众只需改一处)

---

### Rule 4: Explicit > Implicit

**The Pattern**:
```
❌ 错误模式: 期望 Claude 推断你的意图
✅ 正确模式: 显式声明预期行为
```

**Why it works**:
Claude 很强大,但不是读心术。显式声明:
- 行为边界 (做什么 / 不做什么)
- 输出格式
- 成功标准
可以大幅提高成功率。

**How to apply**:

**示例 1: Agent 边界**

```markdown
# ❌ Implicit
---
name: code-reviewer
---
Review the code.

# ✅ Explicit
---
name: code-reviewer
---
## You WILL:
- Check security issues
- Suggest performance improvements
- Verify error handling

## You will NOT:
- Fix issues yourself (only suggest)
- Review files not in git diff
- Run tests (test-runner agent handles this)
```

**示例 2: 输出格式**

```markdown
# ❌ Implicit
Generate a commit message.

# ✅ Explicit
Generate a commit message using this EXACT format:

```
🚀 type(scope): summary (max 50 chars)

- Detail 1
- Detail 2

Files: file1.ts, file2.ts
```

Use emojis: 🚀 feat / 🐛 fix / 📚 docs / ♻️ refactor
```

**示例 3: 成功标准**

```markdown
# ❌ Implicit
Optimize performance.

# ✅ Explicit
Optimize performance. Success criteria:
- Response time < 100ms (currently 500ms)
- Database queries < 5 per request (currently 20)
- Memory usage < 50MB (currently 200MB)

Provide benchmark before/after.
```

---

### Rule 5: 参数化而非硬编码

**The Pattern**:
```
❌ 错误模式: 每个变体创建一个新 Command/Skill
✅ 正确模式: 一个参数化的 Command/Skill
```

**Why it works**:
硬编码会导致组合爆炸:

```
❌ 硬编码:
/deploy-staging
/deploy-production
/deploy-development
/deploy-testing
    → 4 个环境 = 4 个文件

✅ 参数化:
/deploy $ENVIRONMENT
    → 1 个文件处理所有环境
```

**How to apply**:

**Command 参数化**:

```markdown
---
name: deploy
description: Deploy to any environment
---

# Deployment Script

Environment: $ARGUMENTS (default: staging)

## Validation
if [[ ! "$ARGUMENTS" =~ ^(staging|production|development)$ ]]; then
  echo "Invalid environment. Use: staging, production, or development"
  exit 1
fi

## Deploy Steps
1. Run tests
2. Build for $ARGUMENTS
3. Deploy to $ARGUMENTS
4. Smoke test $ARGUMENTS endpoints
```

**Skill 参数化**:

```markdown
---
name: api-client-generator
description: Generate API client code in any language
---

# API Client Generator

Supported languages:
- Python
- TypeScript
- Go
- Rust

## Usage

When user requests: "Generate Python API client for /users endpoint"

1. Detect language from request ($LANG = Python)
2. Load template: templates/$LANG/client.template
3. Generate code
```

---

## 9. Key Takeaways

### For 个人开发者

1. **从 Slash Commands 开始**: 把你每天重复的 Git/测试/部署流程变成 `/cmd` - 投资 30 分钟,节省数百小时
2. **创建个人 Skill 库**: 你的领域知识值得沉淀 (如机械工程的 FEA 分析、振动信号处理) - 让 Claude 成为你的专业助手
3. **用 Sub-Agents 处理脏活累活**: 代码审查、性能分析这些"需要看 100 个文件"的任务,让 Agent 隔离处理,保持主对话清爽

### For 团队/管理者

1. **标准化工作流**: 将 `.claude/skills/` 和 `.claude/agents/` 纳入版本控制 - 新人 `git clone` 后自动获得团队规范
2. **Skills 即知识库**: 公司品牌规范、API 设计标准、安全 Checklist 都应该是 Skills - 让 AI 成为活的文档
3. **并行 Code Review**: 配置 5 个 Agent (质量+安全+性能+测试+文档),PR 审查时间从 1 小时降到 10 分钟

### For 架构师/高级工程师

1. **设计三层架构**: Skills (领域容器) + Commands (嵌套任务) + Agents (独立工作者) - 避免平铺导致的维护噩梦
2. **Context 是稀缺资源**: 主对话的 200K Token 应该用于高层决策,脏活交给 Sub-Agents 的独立 Context
3. **构建插件生态**: 如果你的团队有 10+ 个 Skills,考虑打包成内部插件,通过插件市场分发 - 提高复用性

---

## 10. Glossary

| 术语 | 中文 | 定义 |
|------|------|------|
| **Slash Command** | 斜杠命令 | 用户显式调用的预定义 Prompt 模板,如 `/commit` |
| **Skill** | 技能包 | 模型自动发现的领域能力包,包含 SKILL.md + 支持文件 |
| **Sub-Agent** | 子代理 | 独立上下文的 AI 工作者,执行特定任务后返回结果 |
| **Description** | 描述字段 | Skill/Agent 的关键元数据,决定何时被发现和调用 |
| **Progressive Disclosure** | 渐进式加载 | 先加载 name+description,相关时才加载完整内容 |
| **Context Window** | 上下文窗口 | AI 对话的记忆容量,通常 200K Tokens |
| **Context Pollution** | 上下文污染 | 无关信息占用 Context,导致 Token 浪费 |
| **allowed-tools** | 工具白名单 | 限制 Skill/Agent 可用的工具列表 (如只读权限) |
| **permissionMode** | 权限模式 | Agent 的权限策略 (default/acceptEdits/bypass 等) |
| **Plugin** | 插件 | 打包的 Skills + Agents + Commands 集合,可分享 |
| **Model RAM** | 模型 RAM | 模型可靠跟踪的条件分支数 (Haiku: 3-4, Sonnet: 6-8) |
| **Agent Orchestration** | 代理编排 | 管理多个 Agent 协作完成复杂任务的架构模式 |

---

## 11. Sources

### 官方资源
- [Agent Skills 官方文档](https://code.claude.com/docs/en/skills)
- [Sub-Agents 官方文档](https://code.claude.com/docs/en/sub-agents)
- [Slash Commands SDK 文档](https://platform.claude.com/docs/en/agent-sdk/slash-commands)
- [Claude Code 最佳实践](https://www.anthropic.com/engineering/claude-code-best-practices)
- [Anthropic 官方 Skills 仓库](https://github.com/anthropics/skills)

### 技术博客
- [Understanding Claude Code: Skills vs Commands vs Subagents vs Plugins](https://www.youngleaders.tech/p/claude-skills-commands-subagents-plugins)
- [Claude Agent Skills: A First Principles Deep Dive](https://leehanchung.github.io/blogs/2025/10/26/claude-skills-deep-dive/)
- [Understanding Claude Code's Full Stack: MCP, Skills, Subagents, and Hooks](https://alexop.dev/posts/understanding-claude-code-full-stack/)
- [When to Use Claude Code Skills vs Commands vs Agents](https://danielmiessler.com/blog/when-to-use-skills-vs-commands-vs-agents)
- [Slash Commands vs Subagents: How to Keep AI Tools Focused](https://jxnl.co/writing/2025/08/29/context-engineering-slash-commands-subagents/)

### 教程与实战
- [How to create custom Skills](https://support.claude.com/en/articles/12512198-how-to-create-custom-skills)
- [How to Build Claude Skills: Lesson Plan Generator Tutorial](https://www.codecademy.com/article/how-to-build-claude-skills)
- [Claude Code Slash Commands - Complete Reference Guide](https://firstprinciplescg.com/resources/claude-code-slash-commands-the-complete-reference-guide/)
- [How I Use Every Claude Code Feature](https://blog.sshh.io/p/how-i-use-every-claude-code-feature)

### 社区讨论
- [Awesome Claude Code (GitHub)](https://github.com/hesreallyhim/awesome-claude-code)
- [Claude Code Skill Factory](https://github.com/alirezarezvani/claude-code-skill-factory)
- [SkillsMP Marketplace](https://skillsmp.com/)
- [Awesome Claude Skills](https://github.com/travisvn/awesome-claude-skills)

---

**文档版本**: v1.0.0  
**创建日期**: 2025-12-21  
**作者**: AI Research Team  
**字数**: 约 15,000 字 (550+ 行)
