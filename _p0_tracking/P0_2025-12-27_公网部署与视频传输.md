# P0 追踪：公网部署 + 视频传输

| 项目 | 内容 |
|------|------|
| 日期 | 2025-12-27 |
| P0 任务 | 公网部署 + 视频传输 |
| 状态 | 🟡 **P0.1 完成，P0.2 Firebase MVP 验证通过** |
| 开始时间 | 12:56 |
| 当前时间 | 19:45 |
| 实际用时 | 约 7 小时（含午餐休息 + 文档恢复） |
| 方案演变 | ngrok → Railway Backend → Railway 全栈 → Firebase Storage |

---

## P0 定义

### 任务拆分

| P0 | 任务 | 完成标准 | 状态 |
|----|------|----------|------|
| P0.1 | 公网部署 + 手机验证 | 手机 Safari 能访问 User/Server 页面 | ✅ 完成 |
| P0.2 | 视频传输修复 | 用户手机能看到示范视频 | 🟡 MVP 验证通过，待集成 |

### 任务描述

将 User 端部署到公网，验证手机能通过公网 URL 完成完整流程。

### 背景（基于昨天的工作）

**昨天完成的工作**：
- ✅ Gemini API 自动推荐 MCQ + Boost（原 P0）
- ✅ Node.js Backend 创建（Express，端口 8000）
- ✅ 分离部署重构（User 3000 / Server 3001 / Backend 8000）
- ✅ HTTP 轮询通信（200ms 间隔）
- ✅ 本地局域网 + 手机 WiFi 测试跑通

**昨天遗留的问题**：
- ⚠️ Backend URL 硬编码（localhost:8000）
- ⚠️ 手机 WiFi 访问时 CORS 问题

**当前架构**：

```
物理电脑 (localhost)
├─ User 端 (Vite): 3000 端口
├─ Server 端 (Vite): 3001 端口
└─ Backend (Express): 8000 端口
   └─ HTTP 轮询通信（200ms 间隔）
   └─ Gemini API 调用（gemini-1.5-flash）
```

### 完成标准

| 标准 | 验收方式 | 状态 |
|------|----------|------|
| 1. Backend 公网可访问 | Railway URL 返回 {"status":"ok"} | ✅ 已验证 |
| 2. 前端部署到公网 | ~~Vercel~~ → Railway 全栈部署 | ✅ 已部署 |
| 3. 手机完成完整流程 | 手机 Safari 访问 User/Server 页面 | ✅ 已验证 |
| 4. 视频可传输 | 用户手机能看到示范视频 | 🟡 MVP 验证通过 |

### 部署结果 URL

| 应用 | URL | 状态 |
|------|-----|------|
| User 学员端 | https://tennis-backend-production-b0f3.up.railway.app/user/ | ✅ 200 OK |
| Server 教练端 | https://tennis-backend-production-b0f3.up.railway.app/server/ | ✅ 200 OK |
| Backend API | https://tennis-backend-production-b0f3.up.railway.app/health | ✅ 运行中 |
| ~~Vercel~~ | ~~https://dist-xxx.vercel.app~~ | ❌ 已删除 |

---

## 时间线

### P0.1：公网部署（12:56-16:17）

| 时间 | 事件 | 状态 |
|------|------|------|
| 12:56 | P0 确认，创建追踪文档 | ✅ |
| 13:00 | 用户询问：为什么用 ngrok 而不是 Vercel 部署 Backend | ✅ |
| 13:05 | 监督 Claude 解释：Backend 需要内存队列，Vercel Serverless 无状态 | ✅ |
| 13:08 | 用户提出新规范：对话记录文档（CONV）避免消息爆炸 | ✅ |
| 13:08 | 更新 p0.md 规范 + 创建 CONV_2025-12-27.md | ✅ |
| 13:10 | 用户提出 Railway 方案（生产环境需求） | ✅ |
| 13:15 | 监督 Claude 对比 ngrok vs Railway，推荐 Railway | ✅ |
| ~13:15 | 主 Claude Code 开始执行 ngrok 方案（用户尚未通知变更） | ⚠️ |
| ~13:20 | 主 Claude Code 完成：安装 ngrok + 代码修改（VITE_BACKEND_URL） | ✅ |
| 13:23 | 用户决定切换到 Railway 方案 | ✅ |
| 13:23 | 监督 Claude 给出 Railway 方案 Prompt | ✅ |
| ~13:30 | 用户确认切换 Railway，主 Claude 检查 Backend 配置 | ✅ |
| ~13:32 | 修复 Backend：PORT 环境变量 + CORS 支持 Vercel 域名 | ✅ |
| ~13:35 | 用户用 GitHub 登录注册 Railway | ✅ |
| ~13:38 | 主 Claude 安装 Railway CLI（npm install -g @railway/cli） | ✅ |
| ~13:40 | Railway 登录：用户手动执行 `railway login --browserless` | ✅ |
| ~13:42 | Railway 项目创建：tennis-backend | ✅ |
| ~13:44 | 代码上传到 Railway（railway up --detach） | ✅ |
| ~13:46 | CLI 网络问题：无法通过命令行设置环境变量 | ⚠️ |
| ~13:48 | 用户手动在 Railway 网页设置 GEMINI_API_KEY | ✅ |
| ~13:50 | 用户询问 Railway Build Failed 原因（Root Directory 问题） | ✅ |
| ~13:51 | Backend 健康检查通过：/health 返回 {"status":"ok"} | ✅ |
| ~13:51 | 前端构建成功（npm run build） | ✅ |
| ~13:55 | Vercel 部署成功（vercel --prod） | ✅ |
| 13:59 | P0 追踪文档更新（Round 3） | ✅ |
| ~14:00 | 用户报告：手机访问 Vercel 需要登录（Deployment Protection 问题） | ⚠️ |
| ~14:05 | 用户询问：能否把前端也部署到 Railway | ✅ |
| ~14:10 | 主 Claude 分析 Vercel 404 问题（缺少 index.html + vercel.json） | ✅ |
| ~14:15 | 主 Claude 提出 Railway 全栈部署方案 | ✅ |
| ~14:20 | 用户执行 /commit（803d5d7 Railway 云端部署支持） | ✅ |
| ~14:25 | 用户同意 Railway 全栈部署 + 删除 Vercel 项目 | ✅ |
| ~14:27 | 修改 server.js 添加静态文件服务 | ✅ |
| ~14:28 | 修改 communication.ts + geminiService.ts 支持同源部署 | ✅ |
| ~14:30 | 前端构建 + 复制到 backend/public | ✅ |
| ~14:32 | 重新部署到 Railway（railway up --service tennis-backend） | ✅ |
| ~14:33 | 删除 Vercel 项目（vercel rm dist --yes） | ✅ |
| ~14:35 | 验证通过：Health ✅ / User ✅ / Server ✅ | ✅ |
| 14:40 | P0 追踪文档更新（Round 4） | ✅ |
| ~14:45 | 用户报告前端页面空白（白屏） | ⚠️ |
| ~14:50 | 主 Claude 查看 Railway logs 发现 CORS 错误 | ✅ |
| ~14:55 | 修复：添加 Railway 域名到 CORS 白名单 | ✅ |
| ~15:00 | 重新部署到 Railway（railway up） | ✅ |
| ~15:05 | Git commit 2cebd05 并 push 到 remote | ✅ |
| ~15:10 | 用户报告手机仍无法访问 | ⚠️ |
| ~15:15 | 电脑 curl 测试正常，排查部署状态 | ✅ |
| ~15:20 | 用户午餐休息 | ⏸️ |
| ~16:10 | 用户回来，发现 Railway 需要点击"deploy one new change" | 💡 |
| ~16:15 | 点击部署按钮后，手机 Safari 访问成功 | ✅ |
| ~16:17 | **P0.1 完成：三项完成标准全部达到** | 🎉 |
| 16:18 | P0 追踪文档更新（Round 5） | ✅ |

### P0.2：视频传输修复（16:30-19:01）

| 时间 | 事件 | 状态 |
|------|------|------|
| ~16:30 | 发现新问题：视频无法传输到用户手机 | ⚠️ |
| ~16:45 | 确定 P0.2：视频传输修复 | ✅ |
| ~17:00 | 讨论云存储方案：AWS S3 vs Firebase | ✅ |
| ~17:30 | 选择 Firebase（公司有账号） | ✅ |
| ~17:45 | 发现需要 Blaze Plan（付费计划） | ⚠️ |
| ~18:00 | 配置 Firebase Storage + Billing Account | ✅ |
| ~18:30 | Python SDK MVP 测试脚本创建 | ✅ |
| ~18:50 | **Firebase MVP 验证成功！30MB 视频上传 + 公开 URL 可访问** | 🎉 |
| 19:01 | P0 追踪文档更新（Round 7） | ✅ |

### 文档事故恢复（19:05-19:45）

| 时间 | 事件 | 状态 |
|------|------|------|
| ~19:05 | 用户发现 P0 追踪文档丢失（12-26 + 12-27 原版） | ⚠️ |
| ~19:10 | 定位原因：误删 PR #3 时用了 `--delete-branch`，分支被删除 | ✅ |
| ~19:15 | 找到恢复方案：git reflog 显示 commit `cea8c51` 仍存在 | ✅ |
| ~19:20 | 恢复 7 个丢失文件（P0 文档、track.md、handoff 等） | ✅ |
| ~19:25 | 添加「P0 文档保护规范」到 p0.md，防止再次发生 | ✅ |
| ~19:30 | 制定 12-27 P0 文档合并计划（原版 737 行 + 当前版 221 行） | ✅ |
| ~19:40 | **合并完成！创建完整版 P0 文档（842 行）** | 🎉 |
| ~19:45 | 删除临时文件 `_original.md`，验证合并结果 | ✅ |

---

## 执行记录

### 阶段 1：P0 确认与 MVP 方案（12:56）

**用户的原始需求**：

用户提出了 3 个步骤的工作计划：
1. User 端部署到 Vercel
2. 本地 Backend/Server 24 小时运行
3. 真实用户测试（包括 SOP、留存策略）

**监督 Claude 的判断**：

| 优先级 | 任务 | 说明 |
|-------|------|------|
| **P0** | Vercel 部署 + 自己手机验证 | 技术链路验证 |
| P1 | 找 1 个真实用户测试 | P0 完成后 |
| P2 | 用户测试 SOP | 今天不做 |
| P2 | 产品介绍指导流程 | 今天不做 |
| P2 | 早期用户留存策略 | 今天不做 |

用户确认 P0：**Vercel 部署 + 自己手机验证完整流程**

---

### 阶段 2：方案变更 ngrok → Railway（13:10-13:23）

**13:10 - 用户提出生产环境需求**

用户原话：
> "railway呢？how about it？ngrok会不会太老了？我现在就是要部署到真实的生产环境了"

**监督 Claude 的分析**：

| 特性 | ngrok | Railway |
|------|-------|---------|
| 定位 | 内网穿透（临时测试） | 云平台（生产部署） |
| URL 稳定性 | ❌ 免费版每次重启变化 | ✅ 固定 URL |
| 24/7 运行 | ❌ 依赖电脑开机 | ✅ 云端运行 |
| 生产环境 | ❌ 不适合 | ✅ 适合 |

**推荐结论**：Railway 更适合生产环境

**~13:15-13:20 - 主 Claude Code 执行 ngrok 方案（未及时同步）**

在用户与监督 Claude 讨论 Railway 时，主 Claude Code 已开始执行 ngrok 方案：

**已完成的工作**：
- ✅ 安装 ngrok（winget install Ngrok.Ngrok）
- ✅ 修改 `shared/communication.ts` - 支持 VITE_BACKEND_URL 环境变量
- ✅ 修改 `server/services/geminiService.ts` - 支持 VITE_BACKEND_URL 环境变量
- ✅ 创建 `.env.production` 占位文件

**关键发现**：代码修改（VITE_BACKEND_URL）是通用的，无论用 ngrok 还是 Railway 都需要

**13:23 - 用户决定切换到 Railway**

用户确认使用 Railway 部署 Backend，监督 Claude 提供了给主 Claude Code 的 Prompt。

**方案变更总结**：

| 项目 | 原方案 (ngrok) | 新方案 (Railway) |
|------|---------------|-----------------|
| Backend 部署 | 本地 + ngrok 隧道 | Railway 云端 |
| User 端部署 | Vercel | Vercel（不变） |
| URL 稳定性 | 每次重启变化 | 固定 URL |
| 电脑依赖 | 需要保持开机 | 不需要 |
| 代码改动 | VITE_BACKEND_URL（已完成） | 复用已完成的改动 |

---

### 阶段 3：Railway 部署执行（~13:30-13:55）

**13:30 - 用户确认切换 Railway，主 Claude 开始执行**

用户原话：
> "继续railway部署，让我们stick to MVP build up"

主 Claude Code 检查 Backend 配置，发现 2 个问题需要修复：

| 检查项 | 状态 | 问题 |
|--------|------|------|
| package.json start 脚本 | ✅ | 有 "start": "node server.js" |
| PORT 配置 | ❌ | 硬编码 8000，需要读取 process.env.PORT |
| CORS 配置 | ❌ | 只允许 localhost，需要添加 Vercel 域名 |

**修复内容**：
- `backend/server.js:7` - 改为 `process.env.PORT || 8000`
- `backend/server.js:32-36` - 添加 `*.vercel.app` CORS 匹配

**~13:35 - 用户完成 Railway 注册**

用户用 GitHub 账号登录 Railway。

**~13:38-13:42 - Railway CLI 部署流程**

主 Claude Code 执行：
1. `npm install -g @railway/cli` - 安装成功
2. `railway login` - 需要交互模式，失败
3. 用户手动执行 `railway login --browserless` - 登录成功
4. `railway init --name tennis-backend` - 项目创建成功

Railway 项目链接：
> https://railway.com/project/5562e826-b08d-479d-8584-f3054af3184f

**~13:44 - 代码上传**

```
railway up --detach
→ Build Logs: https://railway.com/project/5562e826-b08d-479d-8584-f3054af3184f/service/88139e39-258e-4fee-a2d7-d5019cf43317
```

**~13:46 - CLI 网络问题**

主 Claude 尝试通过 CLI 设置环境变量，遇到网络错误：

> Failed to fetch: error sending request for url (https://backboard.railway.com/graphql/v2)
> Caused by: tls handshake eof

**~13:48 - 用户手动设置环境变量**

主 Claude 指导用户在 Railway 网页 UI 手动添加 `GEMINI_API_KEY`。

**~13:50 - 用户询问 Railway Build Failed 原因**

用户原话：
> "在我让你自动部署之前我其实自己把这个仓库给import到railway上面但是刚才显示build failed是因为这个service应该是从backend子目录搭建的嘛而它railway默认从仓库根目录构建是这个原因嘛？"

主 Claude 解释：

```
你的仓库结构:
301_tennis-forehand-rubrics/
├─ webapp-v5-api/
├─ webapp-v6-ios-server/
│   └─ backend/           ← 真正的 Node.js 项目在这里
│       ├─ package.json
│       └─ server.js
└─ ...其他文件

Railway 默认行为：从仓库根目录找 package.json，但根目录没有！
```

**解决方案**：
- 网页导入方式：Settings → Source → Root Directory → 填入 `webapp-v6-ios-server/backend`
- CLI 方式：直接 `cd` 到子目录执行 `railway up`

**~13:51 - Railway 部署验证**

主 Claude 执行健康检查：

```
curl https://tennis-backend-production-b0f3.up.railway.app/health
→ {"status":"ok","message":"Backend server is running"}
```

**~13:51-13:55 - 前端构建与 Vercel 部署**

1. 更新 `.env.production`：
   ```
   VITE_BACKEND_URL=https://tennis-backend-production-b0f3.up.railway.app
   ```

2. 构建前端：
   ```
   npm run build
   → dist/user/index.html (26.20 kB)
   → dist/server/index.html (27.13 kB)
   → dist/assets/communication-DF_y7pX5.js (196.59 kB)
   ```

3. 部署到 Vercel：
   ```
   cd dist && vercel --prod --yes
   → Production: https://dist-9q25i9sfy-zhen-zhus-projects.vercel.app
   ```

**部署完成架构（已废弃，见阶段 4）**：

```
┌──────────────────────────────────────────────────────┐
│  Vercel (静态托管) - 24/7 运行                        │
│  https://dist-9q25i9sfy-zhen-zhus-projects.vercel.app│
│  ├─ /user/   → 学员端 (上传视频、回答 MCQ)            │
│  └─ /server/ → 教练端 (AI 分析、生成报告)             │
└───────────────────────┬──────────────────────────────┘
                        │ HTTP 请求
                        ▼
┌──────────────────────────────────────────────────────┐
│  Railway (云端) - 24/7 运行                          │
│  https://tennis-backend-production-b0f3.up.railway.app│
│  ├─ Gemini API 代理                                  │
│  └─ 内存队列通信                                     │
└──────────────────────────────────────────────────────┘
```

---

### 阶段 4：Vercel 问题 → Railway 全栈部署（~14:00-14:35）

**~14:00 - 用户报告 Vercel 访问问题**

用户原话：
> "有一个问题：我在手机上登录 user 和 server 的两个 Vercel 页面它会显示需要我 login Vercel。这应该不太对吧？但是 backend 这个 Railway 我是能正常在手机上用蜂窝移动数据流量去登录打开的。"

**问题分析**：Vercel Deployment Protection 默认开启，导致需要登录才能访问。

**~14:05 - 用户询问 Railway vs Vercel**

用户原话：
> "请问我是不是可以把 user 和 server 也部署到 Railway 上，而不是 Vercel，告诉我哪一个方案更有利于实际生产环境的运行而且对方可以在任何网络下都能连接、打开，而不需要 VPN"

**~14:10 - 主 Claude 分析 Vercel 404 问题**

| 问题 | 原因 |
|------|------|
| 404 NOT_FOUND | Vercel 默认找 `dist/index.html`，但多页应用只有 `dist/user/index.html` 和 `dist/server/index.html` |
| 缺少路由配置 | 多页应用需要 `vercel.json` 配置 rewrites/routes |
| 零配置部署局限 | 直接上传 dist 目录，没有构建过程，静态文件服务配置不完整 |

**~14:15 - Railway 全栈部署方案**

主 Claude 提出新方案：让 Express 服务器同时服务静态文件

```
新方案架构：
┌─────────────────────────────────────┐
│ Railway (全栈)                      │
│ ├─ /user/*     → User 前端          │
│ ├─ /server/*   → Server 前端        │
│ ├─ /api/*      → Backend API        │
│ └─ /health     → 健康检查           │
└─────────────────────────────────────┘
```

**优势对比**：

| 特性 | Vercel + Railway | Railway 全栈 |
|------|-----------------|--------------|
| 部署复杂度 | 高（两个平台） | 低（单一平台） |
| CORS 问题 | 需要配置 | 无（同源） |
| 免费额度 | 分开计算 | 共享 $5/月 |
| 网络访问 | 两个都要能访问 | 只需访问一个 |
| 维护成本 | 高 | 低 |

**~14:20 - 用户执行 /commit**

Git Commit 创建成功：
> 803d5d7 🚀 feat(deploy): Railway 云端部署支持 - 环境变量 + CORS + 生产配置

**~14:25 - 用户同意 Railway 全栈部署**

用户原话：
> "同意之前的 railway 全栈部署方案并且把删除 Vercel 的项目也执行。我同意了开始执行。"

**~14:27-14:30 - 代码修改**

1. **backend/server.js** - 添加静态文件服务（约 40 行代码）
   - 导入 `path` 和 `fileURLToPath`
   - 配置 `/user` 和 `/server` 静态目录
   - 添加 SPA 路由回退（返回 index.html）

2. **communication.ts** - 支持同源部署
   - 检测 Railway 域名时返回空字符串（使用相对路径）

3. **geminiService.ts** - 支持同源部署
   - 同样检测 Railway 域名

**~14:30 - 前端构建与复制**

```
npm run build
→ dist/user/index.html (26.20 kB)
→ dist/server/index.html (27.21 kB)
→ dist/assets/communication-DMxoufE4.js (196.71 kB)

cp -r dist/* backend/public/
```

**~14:32 - 重新部署到 Railway**

```
railway up --service tennis-backend --detach
→ Build Logs: https://railway.com/project/5562e826.../service/88139e39...
```

**~14:33 - 删除 Vercel 项目**

```
vercel rm dist --yes
→ Success! Removed 1 project
```

**~14:35 - 验证通过**

| 页面 | URL | 状态 |
|------|-----|------|
| User 学员端 | https://tennis-backend-production-b0f3.up.railway.app/user/ | ✅ 200 OK |
| Server 教练端 | https://tennis-backend-production-b0f3.up.railway.app/server/ | ✅ 200 OK |
| 健康检查 | https://tennis-backend-production-b0f3.up.railway.app/health | ✅ 运行中 |

**最终架构（Railway 全栈）**：

```
┌─────────────────────────────────────────────────────────────┐
│  Railway (单一服务，24/7 运行)                              │
│  https://tennis-backend-production-b0f3.up.railway.app      │
│                                                             │
│  ├─ /user/      → User 学员端 (静态前端)                    │
│  ├─ /server/    → Server 教练端 (静态前端)                  │
│  ├─ /api/*      → Backend API                               │
│  │   ├─ /api/gemini/*    → Gemini AI 代理                   │
│  │   └─ /api/comm/*      → 队列通信                         │
│  └─ /health     → 健康检查                                  │
└─────────────────────────────────────────────────────────────┘
```

---

### 阶段 5：CORS 修复 + 部署确认（~14:45-16:17）

这个阶段经历了问题发现、修复、再次遇阻、最终解决的完整过程。

**~14:45 - 用户报告前端页面空白**

用户反馈：
> 部署完成后，手机访问 User/Server 页面显示空白（白屏）。

**~14:50 - 主 Claude 查看 Railway Logs**

主 Claude 检查 Railway 日志，发现关键错误：

> `CORS not allowed for origin: https://tennis-backend-production-b0f3.up.railway.app`

**问题分析**：

| 项目 | 分析 |
|------|------|
| 现象 | 前端页面白屏，无内容显示 |
| 根因 | Railway 域名 (`*.railway.app`) 不在 CORS 白名单中 |
| 影响 | 前端 JS 请求后端 API 被浏览器 CORS 策略拦截 |

**~14:55 - CORS 修复**

主 Claude 修改 `backend/server.js`，添加 Railway 域名匹配：

```javascript
const railwayPattern = /^https:\/\/.*\.railway\.app$/;
```

**~15:00 - 重新部署**

执行 `railway up` 重新部署包含 CORS 修复的代码。

**~15:05 - Git Commit**

创建 commit 并推送：
> 2cebd05 - CORS 修复 + Railway 域名支持

**~15:10 - 第二次问题：手机仍无法访问**

用户反馈：
> 电脑 curl 测试都正常，但手机 Safari 仍然打不开页面。

主 Claude 排查：
- 电脑 curl 健康检查：✅ 正常
- 电脑 curl User 页面：✅ 正常
- 手机 Safari 访问：❌ 仍无法访问

**~15:20 - 用户午餐休息**

排查暂停，用户去吃午餐。

**~16:10 - 关键发现：Railway 部署确认机制**

用户午餐回来后，在 Railway Dashboard 发现：
> 界面上有一个 "deploy one new change" 按钮需要点击确认

**Railway 部署机制解释**（主 Claude 说明）：

| 场景 | 行为 |
|------|------|
| `railway up` 不带 `--detach` | CLI 等待部署完成，自动确认 |
| `railway up --detach` | CLI 立即返回，需要手动确认或等待 |
| Dashboard 显示 pending change | 需要手动点击"deploy"按钮 |

**~16:15 - 点击部署按钮**

用户在 Railway Dashboard 点击部署按钮，几秒后部署完成。

**~16:17 - P0.1 完成验证**

用户手机 Safari 验证：
- ✅ User 页面：https://tennis-backend-production-b0f3.up.railway.app/user/ - 正常打开
- ✅ Server 页面：https://tennis-backend-production-b0f3.up.railway.app/server/ - 正常打开

**三项完成标准全部达成，P0.1 完成！** 🎉

---

### 阶段 6-7：Firebase Storage 视频托管（16:30-18:50）

**问题发现**：

P0.1 完成后，用户手机能访问页面，但看不到示范视频（正确/错误示范）。

**根因分析**：

| 分析项 | 结论 |
|--------|------|
| 视频文件位置 | 本地 `backend/public/videos/` |
| Railway 部署 | 没有包含视频文件 |
| 传输机制 | Server 端发送视频给 User 端 |
| 问题 | 视频未部署到云端，无法传输 |

**解决方案选型**：

| 方案 | 优点 | 缺点 | 选择 |
|------|------|------|------|
| Railway 存储 | 简单 | 占用 Railway 额度 | ❌ |
| AWS S3 | 专业 CDN | 配置复杂 | ❌ |
| Firebase Storage | 简单 + 公司有账号 | 需要 Blaze Plan | ✅ |

**Firebase 配置过程**：

1. 创建项目：`yoach-b235c`
2. 发现需要 Blaze Plan（付费计划，但有免费额度）
3. 配置 Cloud Billing Account
4. 启用 Storage 服务
5. 下载 Service Account Key
6. 配置 Storage Rules（公开读取）

**MVP 验证结果**：

```
┌─────────────────────────────────────────────────────────┐
│  Firebase Storage MVP 验证 - 成功！                     │
├─────────────────────────────────────────────────────────┤
│  ✅ 服务账号密钥可用                                    │
│  ✅ Google Cloud Storage SDK 初始化成功                 │
│  ✅ 30.67 MB 视频上传成功                               │
│  ✅ 公开 URL 可访问 (Status 200)                        │
│                                                         │
│  项目 ID: yoach-b235c                                   │
│  Bucket: yoach-b235c.firebasestorage.app               │
│  测试 URL: https://firebasestorage.googleapis.com/...  │
└─────────────────────────────────────────────────────────┘
```

---

### 阶段 8：P0 文档事故恢复（19:05-19:45）

**事故发现**：

用户在 19:05 发现 P0 追踪文档丢失，包括：
- `P0_2025-12-26_Gemini自动推荐MCQ.md`（昨天的完整记录）
- `P0_2025-12-27_Vercel部署公网验证.md`（原版，Round 1-5 详细记录）

**根因分析**：

| 分析项 | 结论 |
|--------|------|
| 触发动作 | 用户要求删除错误创建的 PR #3 |
| 错误操作 | 执行 `gh pr close 3 --delete-branch` |
| 后果 | 分支 `docs/p0-tracking-update-2025-12-27` 被删除 |
| 影响 | 分支上的 P0 文档未合并到 main，随分支一起丢失 |

**恢复方案**：

Git reflog 显示 commit `cea8c51` 仍存在于本地对象库，可通过 `git show` 恢复。

**恢复的文件（7 个）**：

| 文件 | 说明 |
|------|------|
| `P0_2025-12-26_Gemini自动推荐MCQ.md` | 12-26 P0 追踪 |
| `P0_2025-12-27_Vercel部署公网验证.md` | 12-27 原版（保存为 `_original.md`） |
| `.claude/commands/track.md` | P0 追踪 Agent Skill |
| `_handoff/Tracking_Agent_Handoff_2025-12-26.md` | Agent 交接文档 |
| `raw/EXEC_MCQ-dual-console-03-walkthrough.md` | 执行记录 |
| `raw/PROMPT_MCQ-admin-server-v1.5.1.md` | Prompt 文档 |
| `raw/PROMPT_MCQ-communication-MVP-v1.1.md` | Prompt 文档 |

**预防措施**：

添加「P0 文档保护规范」到 `.claude/commands/p0.md`：

| 规则 | 说明 |
|------|------|
| P0 文档必须在 main 分支 | 不在 feature 分支存放 |
| 删除分支前必须确认 | 检查是否有未合并的 P0 文档 |
| 不用 `--delete-branch` | 分步操作更安全 |
| 定期 commit | 每 1-2 小时提交一次 |

**文档合并**：

将两个版本合并为完整版：

| 来源 | 内容 | 行数 |
|------|------|------|
| 原版 | Round 1-5 详细记录 | 737 行 |
| 当前版 | Round 6-7 Firebase 记录 | 221 行 |
| 合并后 | 完整 P0 追踪文档 | 842+ 行 |

**教训总结**：

```
┌─────────────────────────────────────────────────────────┐
│  P0 文档保护 - 核心原则                                  │
├─────────────────────────────────────────────────────────┤
│  1. P0 文档始终在 main 分支                              │
│  2. 删除分支前检查未合并内容                             │
│  3. 破坏性操作分步执行                                   │
│  4. Git reflog 是最后救命稻草                            │
└─────────────────────────────────────────────────────────┘
```

---

## 技术要点学习

### Railway Root Directory 问题

**问题**：从 GitHub 导入 monorepo 时，Railway 默认从仓库根目录构建，导致 Build Failed。

**原因**：仓库根目录没有 `package.json`，真正的 Node.js 项目在子目录 `webapp-v6-ios-server/backend/`。

**解决方案**：

| 方式 | 操作 |
|------|------|
| 网页 UI | Settings → Source → Root Directory → `webapp-v6-ios-server/backend` |
| CLI | `cd webapp-v6-ios-server/backend && railway up`（直接从子目录上传） |

### PORT 环境变量

Railway 自动注入 `PORT` 环境变量，代码需要读取：

```javascript
const PORT = process.env.PORT || 8000;
```

### Vercel Deployment Protection 问题

**问题**：Vercel 部署后，手机访问需要登录 Vercel 账号。

**原因**：Vercel 默认开启 Deployment Protection，需要在设置中关闭。

**教训**：直接使用 Railway 全栈部署可以避免这个问题。

### Vercel 多页应用部署问题

**问题**：访问 Vercel 域名显示 404 NOT_FOUND。

**原因**：
1. Vercel 默认找 `dist/index.html`，但多页应用只有 `dist/user/index.html`
2. 缺少 `vercel.json` 路由配置
3. 零配置部署局限

**解决方案**：不用 Vercel，改用 Railway 全栈部署。

### Express 静态文件服务

让 Express 同时服务 API 和静态前端：

```javascript
import { fileURLToPath } from 'url';
import path from 'path';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// 静态文件服务
app.use('/user', express.static(path.join(__dirname, 'public/user')));
app.use('/server', express.static(path.join(__dirname, 'public/server')));

// SPA 路由回退
app.get('/user/*', (req, res) => {
  res.sendFile(path.join(__dirname, 'public/user/index.html'));
});
```

### 同源部署优势

| 特性 | 分离部署 (Vercel + Railway) | 同源部署 (Railway 全栈) |
|------|---------------------------|------------------------|
| CORS | 需要配置 | 不需要 |
| 部署复杂度 | 高 | 低 |
| 调试 | 难（跨域问题） | 简单 |
| 维护成本 | 高 | 低 |

### Railway CORS 配置

**问题**：同源部署后，前端仍然白屏。

**原因**：虽然是同源部署，但 CORS 中间件在 API 请求前执行，需要显式允许 Railway 域名。

**修复**：在 `server.js` 添加 Railway 域名匹配：

```javascript
const railwayPattern = /^https:\/\/.*\.railway\.app$/;
```

**教训**：即使是同源部署，也要确保 CORS 白名单包含部署域名。

### Railway 部署确认机制

**问题**：代码部署后，手机仍无法访问，但电脑 curl 测试正常。

**原因**：使用 `railway up --detach` 时，CLI 立即返回，但部署可能处于 pending 状态。

**Railway CLI 部署模式对比**：

| 命令 | 行为 | 是否自动确认 |
|------|------|-------------|
| `railway up` | 阻塞等待部署完成 | ✅ 是 |
| `railway up --detach` | 立即返回，后台部署 | ❌ 可能需要手动确认 |

**解决方案**：
1. 使用 `railway up`（不带 `--detach`）等待部署完成
2. 或在 Railway Dashboard 手动点击 "deploy" 按钮确认

**教训**：使用 `--detach` 提高效率时，需检查 Dashboard 确认部署状态。

### Firebase Storage vs AWS S3

| 特性 | Firebase Storage | AWS S3 |
|------|-----------------|--------|
| 配置复杂度 | ⭐ 简单 | ⭐⭐⭐ 复杂 |
| 免费额度 | 5GB 存储 + 1GB/天下载 | 几乎无免费 |
| URL 格式 | 较长 (带 token) | 简洁 |
| 归属 | Google Cloud | Amazon |

### MVP 验证哲学

```
核心原则：先用最小代价验证核心假设

本次实践：
1. 不直接集成到项目
2. 用 Python SDK 独立测试
3. 验证成功后再集成

好处：
- 如果失败，快速调整方向
- 不浪费时间在项目改动上
```

---

## 待办事项

### P0.1 公网部署（✅ 已完成）
- ✅ 代码修改：支持 VITE_BACKEND_URL
- ✅ 修复 Backend：PORT 环境变量 + CORS 支持
- ✅ Railway 部署 Backend
- ✅ Railway 全栈部署（前后端同源）
- ✅ 删除 Vercel 项目
- ✅ CORS 修复：添加 Railway 域名白名单
- ✅ 手机测试完整流程（Safari 验证通过）

### P0.2 视频传输（🟡 进行中）
- ✅ Firebase Storage MVP 验证（通过）
- ⏳ 上传所有 ATOMS/Boost 视频到 Firebase
- ⏳ 修改代码中的视频路径
- ⏳ 重新构建并部署到 Railway
- ⏳ 手机验证视频播放

---

## 部署架构（最终）

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  最终部署架构                                                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  用户手机 (4G/WiFi)                                                             │
│       │                                                                         │
│       ▼                                                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Railway 全栈 (24/7 运行)                                                │   │
│  │  https://tennis-backend-production-b0f3.up.railway.app                   │   │
│  │  ├─ /user/      → User 学员端                                           │   │
│  │  ├─ /server/    → Server 教练端                                         │   │
│  │  ├─ /api/*      → Backend API (Gemini 代理 + 队列通信)                   │   │
│  │  └─ /health     → 健康检查                                              │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│       │                                                                         │
│       │ 视频 URL 请求                                                           │
│       ▼                                                                         │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Firebase Storage (Google Cloud)                                         │   │
│  │  https://firebasestorage.googleapis.com/v0/b/yoach-b235c.../o/...       │   │
│  │  └─ 示范视频 (正确/错误/Boost)                                           │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 相关文件

### P0 追踪
- `_p0_tracking/CONV_2025-12-27.md` - 今日对话记录（9500+ 行）
- `_p0_tracking/P0_2025-12-26_Gemini自动推荐MCQ.md` - 昨天的完整记录

### 项目文件
- `webapp-v6-ios-server/user/` - User 端代码
- `webapp-v6-ios-server/server/` - Server 端代码
- `webapp-v6-ios-server/backend/` - Backend 代码
- `webapp-v6-ios-server/backend/public/` - 构建后的前端静态文件
- `webapp-v6-ios-server/shared/communication.ts` - 通信模块（支持同源部署）
- `webapp-v6-ios-server/.env.production` - 生产环境变量配置

### 云服务链接
- Railway 项目：https://railway.com/project/5562e826-b08d-479d-8584-f3054af3184f
- 生产 URL：https://tennis-backend-production-b0f3.up.railway.app
- Firebase 项目：`yoach-b235c`
- Firebase Storage Bucket：`yoach-b235c.firebasestorage.app`

---

## P0 完成总结

### 完成状态

| 项目 | 结果 |
|------|------|
| 开始时间 | 12:56 |
| 当前时间 | 19:45 |
| 总用时 | 约 7 小时（含午餐休息 + 文档恢复） |
| 方案变更次数 | 3 次（ngrok → Railway → Railway 全栈 → Firebase） |
| 遇到问题数 | 6 个（Vercel Protection、404、CORS、部署确认、视频传输、P0 文档丢失） |
| 文档恢复 | 7 个文件从 git reflog 恢复 |

### 当前成果

**P0.1 公网部署（✅ 完成）**：
- User 学员端：https://tennis-backend-production-b0f3.up.railway.app/user/
- Server 教练端：https://tennis-backend-production-b0f3.up.railway.app/server/
- 健康检查：https://tennis-backend-production-b0f3.up.railway.app/health

**P0.2 视频传输（🟡 MVP 验证通过）**：
- Firebase Storage 已配置
- 30MB 视频上传测试成功
- 公开 URL 可访问
- 待集成到主项目

### 关键经验

| 经验 | 说明 |
|------|------|
| Railway 全栈优于 Vercel + Railway 分离 | 同源部署避免 CORS 问题，部署简单 |
| `--detach` 模式需检查 Dashboard | 可能需要手动确认部署 |
| CORS 白名单要包含部署域名 | 即使同源也要配置 |
| MVP 验证优先 | 遇到问题快速切换方案，先验证再集成 |
| Firebase 比 S3 更适合快速验证 | 配置简单，有免费额度 |
| **P0 文档必须在 main 分支** | 不放 feature 分支，避免误删丢失 |
| **破坏性操作分步执行** | 不用 `--delete-branch`，先检查再删除 |
| **git reflog 是救命稻草** | 删除分支后仍可恢复未 push 的 commit |

### 下一步

P0.2 视频集成：
- 上传所有示范视频到 Firebase Storage
- 修改代码中的视频路径
- 重新部署并验证
