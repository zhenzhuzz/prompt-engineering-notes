# Claude Code Sub-Agents: 构建专业化 AI 团队的完整指南

> **Source**: Claude Code Official Documentation (2025-12)
> **URL**: https://code.claude.com/docs/en/sub-agents
> **Author**: Anthropic
> **Core Theme**: 如何通过 Sub-Agents 实现任务委派、上下文隔离和专业化分工

---

## One Paragraph Takeaway (一段话精华)

**反直觉洞见**: Sub-Agents 的核心价值不是"让 AI 做更多事"，而是**保护主对话的上下文纯净度**。

每个 Sub-Agent 拥有独立的 context window，这意味着探索性搜索、调试日志、代码审查等"脏活"不会污染你的主对话。真正的杠杆在于：让主 Agent 专注于高层决策，将具体执行委派给专业化的 Sub-Agents。如果你让一个 Agent 做所有事情，你很快就会遇到 context 爆炸的问题。

**核心公式**:
> 上下文隔离 × 专业化提示词 = 更高的任务成功率 + 更长的有效会话

---

## The Essence (核心精华)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│              CLAUDE CODE 扩展体系: Sub-Agents + Slash Commands               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   [用户请求]                                                                 │
│       │                                                                     │
│       ├─────────────────────────────────┐                                   │
│       ▼                                 ▼                                   │
│   ┌───────────────────┐         ┌───────────────────┐                       │
│   │  📝 /command      │         │  🤖 Sub-Agents    │                       │
│   │  用户主动触发      │         │  Claude 自动委派   │                       │
│   │  标准化工作流      │         │  专业化任务        │                       │
│   └───────────────────┘         └───────────────────┘                       │
│       │                                 │                                   │
│       ▼                                 ▼                                   │
│   ┌───────────────────┐         ┌───────────────────────────────────────┐   │
│   │ /commit /pr       │         │  MAIN AGENT (主 Agent)                │   │
│   │ /branch           │         │  • 高层决策和任务规划                  │   │
│   │ 在主对话中执行     │         │  • 保持干净的上下文                    │   │
│   └───────────────────┘         └───────────────────────────────────────┘   │
│                                         │                                   │
│                         ┌───────────────┼───────────────┐                   │
│                         ▼               ▼               ▼                   │
│                    ┌─────────┐    ┌─────────┐    ┌─────────┐               │
│                    │ Explore │    │  Plan   │    │ Custom  │               │
│                    │ (Haiku) │    │(Sonnet) │    │  Agent  │               │
│                    │ 只读搜索 │    │ 计划研究 │    │ 你定义的 │               │
│                    └─────────┘    └─────────┘    └─────────┘               │
│                                                                             │
│   ─────────────────────────────────────────────────────────────────────     │
│   💡 核心洞见:                                                               │
│   • Sub-Agent: 独立 context → 探索不污染主对话 → 更长有效会话               │
│   • Commands: 标准化流程 → 一致的输出格式 → 团队协作效率                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**关键数据速览**:

| 维度 | 内置 Agent | 自定义 Agent |
|------|-----------|-------------|
| **存储位置** | 系统内置 | `.claude/agents/` (项目) 或 `~/.claude/agents/` (用户) |
| **优先级** | 最低 | 项目级 > CLI > 用户级 > 内置 |
| **模型选择** | 固定 (Haiku/Sonnet) | 可配置 (`haiku`/`sonnet`/`opus`/`inherit`) |
| **工具权限** | 预设 | 可限制或继承全部 |

---

## Executive Summary

Claude Code 的 Sub-Agents 系统是一个**任务委派架构**，让主 Agent 可以将专业任务分配给预配置的子代理。这不仅仅是"多个 AI 协作"的概念——它解决了 LLM 应用中最关键的问题之一：**上下文管理**。

**三大核心价值**:
1. **Context Preservation (上下文保护)** — 脏活在独立窗口完成，主对话保持干净
2. **Specialized Expertise (专业化)** — 每个 Agent 有针对性的 system prompt
3. **Tool Sandboxing (工具沙箱)** — 可限制 Agent 的工具访问权限

---

## 第一部分：内置 Sub-Agents 深度解析

### 1.1 Explore Agent — 快速只读搜索

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         EXPLORE AGENT 工作模式                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   模型: Haiku (快速、低延迟)                                                 │
│   模式: 严格只读 — 不能创建、修改、删除文件                                   │
│                                                                             │
│   可用工具:                          彻底程度:                               │
│   • Glob  - 文件模式匹配              • Quick  - 快速定向查找                │
│   • Grep  - 内容正则搜索              • Medium - 中等深度探索                │
│   • Read  - 读取文件内容              • Very thorough - 全面分析             │
│   • Bash  - 只读命令 (ls, git等)                                            │
│                                                                             │
│   典型场景:                                                                  │
│   "Where are errors from the client handled?"                               │
│   "What's the codebase structure?"                                          │
│   "Find all API endpoints"                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**为什么用 Haiku**: 搜索任务不需要强推理能力，Haiku 的低延迟让探索过程更快。

**关键洞见**: Explore Agent 找到的内容**不会进入主对话的 context**，这是它最大的价值。

---

### 1.2 Plan Agent — 计划模式专用

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          PLAN AGENT 工作模式                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   模型: Sonnet (更强分析能力)                                                │
│   触发: 仅在 Plan Mode 下自动使用                                            │
│                                                                             │
│   可用工具:                          目的:                                   │
│   • Read  - 读取文件                  • 研究代码库结构                        │
│   • Glob  - 文件搜索                  • 收集实现计划所需的上下文               │
│   • Grep  - 内容搜索                  • 防止 Agent 无限嵌套                   │
│   • Bash  - 探索命令                    (Sub-Agent 不能再召唤 Sub-Agent)      │
│                                                                             │
│   典型流程:                                                                  │
│   User: [Plan Mode] Help me refactor authentication                         │
│     └─▶ Plan Agent 探索 auth 相关代码                                       │
│         └─▶ 返回发现给主 Agent                                              │
│             └─▶ 主 Agent 基于发现制定计划                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 1.3 General-Purpose Agent — 多步复杂任务

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     GENERAL-PURPOSE AGENT 工作模式                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   模型: Sonnet (更强推理)                                                    │
│   模式: 可读写 — 完整工具访问                                                │
│                                                                             │
│   与 Explore 的区别:                                                         │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Explore Agent          │ General-Purpose Agent                      │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ 只读模式                │ 可修改文件                                 │   │
│   │ Haiku (快)             │ Sonnet (强)                                │   │
│   │ 简单搜索任务            │ 多步骤复杂任务                             │   │
│   │ 结果不影响代码          │ 可以执行修改                               │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   典型场景:                                                                  │
│   "Find all places where we handle auth and update them to new format"      │
│   (需要搜索 + 理解 + 修改的复合任务)                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 第二部分：创建自定义 Sub-Agent

### 2.1 文件格式与配置

```markdown
---
name: your-sub-agent-name        # 必填: 小写字母+连字符
description: When to use this    # 必填: 何时使用的自然语言描述
tools: Read, Grep, Glob, Bash    # 可选: 省略则继承全部工具
model: sonnet                    # 可选: haiku/sonnet/opus/inherit
permissionMode: default          # 可选: 权限模式
skills: skill1, skill2           # 可选: 自动加载的技能
---

你的 system prompt 写在这里。
可以是多段落，详细定义 Agent 的角色、能力和方法。
```

**配置字段详解**:

| 字段 | 必填 | 说明 |
|------|------|------|
| `name` | ✅ | 唯一标识符，小写+连字符 |
| `description` | ✅ | **关键**: 决定 Claude 何时自动调用此 Agent |
| `tools` | ❌ | 不填=继承全部；填写=只能用这些 |
| `model` | ❌ | `inherit` 会使用主对话的模型 |
| `permissionMode` | ❌ | `default`/`acceptEdits`/`bypassPermissions`/`plan`/`ignore` |
| `skills` | ❌ | Agent 启动时自动加载的技能 |

### 2.2 存储位置与优先级

```
优先级从高到低:
├── 项目级 Agent: .claude/agents/your-agent.md     ← 最高优先级
├── CLI 定义:     --agents '{...}'                 ← 次高
├── 用户级 Agent: ~/.claude/agents/your-agent.md  ← 次低
└── 内置 Agent:   Explore, Plan, General          ← 最低
```

**实战建议**:
- 团队共享的 Agent → 放 `.claude/agents/`，提交到 git
- 个人偏好的 Agent → 放 `~/.claude/agents/`

### 2.3 CLI 动态定义 (适合测试)

```bash
claude --agents '{
  "code-reviewer": {
    "description": "Expert code reviewer. Use proactively after code changes.",
    "prompt": "You are a senior code reviewer...",
    "tools": ["Read", "Grep", "Glob", "Bash"],
    "model": "sonnet"
  }
}'
```

**适用场景**: 快速测试、临时任务、自动化脚本、文档分享

---

## 第三部分：最佳实践与生产案例

### 3.1 Code Reviewer Agent (代码审查)

```markdown
---
name: code-reviewer
description: Expert code review specialist. Proactively reviews code for quality,
             security, and maintainability. Use immediately after writing or
             modifying code.
tools: Read, Grep, Glob, Bash
model: inherit
---

You are a senior code reviewer ensuring high standards of code quality and security.

When invoked:
1. Run git diff to see recent changes
2. Focus on modified files
3. Begin review immediately

Review checklist:
- Code is clear and readable
- Functions and variables are well-named
- No duplicated code
- Proper error handling
- No exposed secrets or API keys
- Input validation implemented
- Good test coverage
- Performance considerations addressed

Provide feedback organized by priority:
- Critical issues (must fix)
- Warnings (should fix)
- Suggestions (consider improving)

Include specific examples of how to fix issues.
```

**关键设计点**:
- `model: inherit` — 使用主对话的模型，保持一致性
- `description` 包含 "proactively" — 鼓励 Claude 主动调用
- 明确的检查清单 — 确保输出结构化

### 3.2 Debugger Agent (调试专家)

```markdown
---
name: debugger
description: Debugging specialist for errors, test failures, and unexpected
             behavior. Use proactively when encountering any issues.
tools: Read, Edit, Bash, Grep, Glob
---

You are an expert debugger specializing in root cause analysis.

When invoked:
1. Capture error message and stack trace
2. Identify reproduction steps
3. Isolate the failure location
4. Implement minimal fix
5. Verify solution works

For each issue, provide:
- Root cause explanation
- Evidence supporting the diagnosis
- Specific code fix
- Testing approach
- Prevention recommendations

Focus on fixing the underlying issue, not the symptoms.
```

**关键设计点**:
- 包含 `Edit` 工具 — 可以直接修复问题
- 5 步标准流程 — 确保系统性调试
- 强调 "root cause" — 避免表面修复

### 3.3 Data Scientist Agent (数据分析)

```markdown
---
name: data-scientist
description: Data analysis expert for SQL queries, BigQuery operations, and
             data insights. Use proactively for data analysis tasks and queries.
tools: Bash, Read, Write
model: sonnet
---

You are a data scientist specializing in SQL and BigQuery analysis.

When invoked:
1. Understand the data analysis requirement
2. Write efficient SQL queries
3. Use BigQuery command line tools (bq) when appropriate
4. Analyze and summarize results
5. Present findings clearly

For each analysis:
- Explain the query approach
- Document any assumptions
- Highlight key findings
- Suggest next steps based on data

Always ensure queries are efficient and cost-effective.
```

---

## 第四部分：高级用法

### 4.1 Agent 链式调用

```
> First use the code-analyzer subagent to find performance issues,
  then use the optimizer subagent to fix them
```

这会触发两个 Agent 顺序执行，每个都有独立的 context。

### 4.2 可恢复的 Agent (Resumable)

```
初次调用:
> Use the code-analyzer agent to start reviewing the authentication module
[Agent 完成初步分析，返回 agentId: "abc123"]

恢复继续:
> Resume agent abc123 and now analyze the authorization logic as well
[Agent 继续执行，保留之前的完整上下文]
```

**技术细节**:
- 每次执行分配唯一 `agentId`
- 对话存储在 `agent-{agentId}.jsonl`
- 恢复时自动加载之前的完整上下文

**适用场景**:
- 大型代码库分析（分多次完成）
- 迭代优化（不丢失上下文）
- 多步工作流（保持连续性）

### 4.3 鼓励主动调用的技巧

在 `description` 中使用这些短语:
- `"Use PROACTIVELY"`
- `"MUST BE USED when..."`
- `"Use immediately after..."`

**Before vs After**:

| ❌ 被动描述 | ✅ 主动描述 |
|------------|-----------|
| "Reviews code for quality" | "Proactively reviews code. Use immediately after writing code." |
| "Helps with debugging" | "MUST BE USED when encountering any errors or test failures." |

---

## 第五部分：自定义 Slash Commands 实战案例

> 💡 **Slash Commands vs Sub-Agents**:
> - **Slash Commands** (`/commit`, `/pr`) — 用户主动触发的标准化工作流
> - **Sub-Agents** — Claude 自动或按需委派的专业化任务

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    📁 .claude/ 目录结构                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   .claude/                                                                  │
│   ├── 📄 CLAUDE.md              ← 项目级系统提示词                           │
│   ├── 📄 settings.local.json    ← 本地配置                                   │
│   ├── 📁 agents/                ← Sub-Agents 定义 (见第二部分)               │
│   │   └── your-agent.md                                                     │
│   └── 📁 commands/              ← Slash Commands 定义 ⭐                     │
│       ├── 📄 commit.md          ← /commit 命令                              │
│       ├── 📄 pr.md              ← /pr 命令                                  │
│       └── 📄 branch.md          ← /branch 命令                              │
│                                                                             │
│   使用方式: 在对话中输入 /commit, /pr, /branch 即可触发                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5.2 /commit — 标准化提交流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         /commit 工作流程                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户输入: /commit                                                          │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 1: 信息收集 (并行执行)                                          │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • git status        → 查看所有更改                                   │   │
│   │ • git diff --stat   → 改动概览                                       │   │
│   │ • git log -3        → 最近提交风格                                   │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 2: 生成 Commit Message                                          │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • 分析所有 staged + unstaged 更改                                    │   │
│   │ • 按规范生成: emoji + 中文标题 + ASCII 图 + 详细描述                 │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 3: 执行提交                                                     │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • git add [相关文件]                                                 │   │
│   │ • git commit -m "..."                                                │   │
│   │ • git push                                                           │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Commit Message 规范**:

| Emoji | 类型 | 说明 | 示例 |
|-------|------|------|------|
| 🚀 | feat | 新功能 | 🚀 feat(auth): 新增 OAuth 登录 |
| 🐛 | fix | Bug 修复 | 🐛 fix(api): 修复空指针异常 |
| 📚 | docs | 文档更新 | 📚 docs: 更新 README |
| 🔧 | chore | 杂项/配置 | 🔧 chore: 更新依赖 |
| ♻️ | refactor | 重构 | ♻️ refactor(core): 优化缓存逻辑 |
| 🎨 | style | 样式/格式 | 🎨 style: 格式化代码 |
| ✅ | test | 测试 | ✅ test: 添加单元测试 |

**Commit Message 模板**:

```
[emoji] [类型](scope): 一句话标题

═══════════════════════════════════════════════════════════════════
📋 变更摘要
═══════════════════════════════════════════════════════════════════

[详细描述本次提交的所有更改]

═══════════════════════════════════════════════════════════════════
🏗️ 结构/架构变化 (如适用)
═══════════════════════════════════════════════════════════════════

[ASCII diagram 描述文件结构、组件关系、数据流等]

═══════════════════════════════════════════════════════════════════
📁 文件清单
═══════════════════════════════════════════════════════════════════

[列出主要更改的文件及其用途]

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

**⚠️ 注意事项**:
- 如果 push 失败 (HTTP 408)，先诊断是否有大文件，不要盲目重试
- 检查 .gitignore 是否正确排除了视频等大文件

---

### 5.3 /pr — 标准化 PR 创建

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          /pr 工作流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户输入: /pr                                                              │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 1: 分支状态检查 (并行执行)                                       │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • git status                     → 当前分支状态                      │   │
│   │ • git log main..HEAD --oneline   → 待合并的 commits                  │   │
│   │ • git diff main..HEAD --stat     → 改动文件概览                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 2: 检查远程同步                                                  │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • 检查分支是否已推送                                                 │   │
│   │ • 未推送 → 先执行 git push -u origin [branch]                        │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 3: 创建 PR                                                       │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • 分析所有 commits (不仅是最新的!)                                   │   │
│   │ • 生成 PR 标题 + 详细 body                                           │   │
│   │ • gh pr create --title "..." --body "..."                            │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**PR Body 模板**:

```markdown
## 📋 变更摘要

[bullet points 列出主要更改]

## 🏗️ 架构/结构变化

[ASCII diagram 描述架构变化，如适用]

```
文件结构示意:
├── 新增文件...
├── 修改文件...
└── 删除文件...
```

## 📁 主要文件说明

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| xxx.md | 新增 | xxx |
| xxx.js | 修改 | xxx |

## ✅ 测试计划

- [ ] 测试项 1
- [ ] 测试项 2

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

---

### 5.4 /branch — 交互式分支创建

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        /branch 工作流程                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户输入: /branch                                                          │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 1: 环境检查                                                     │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • git status     → 确认工作区状态                                    │   │
│   │ • git branch -a  → 查看现有分支                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       ▼                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 2: 提供建议 ⚠️ 需要用户确认                                      │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │                                                                     │   │
│   │   🌿 分支命名建议                                                   │   │
│   │                                                                     │   │
│   │   根据您的描述，建议以下分支名：                                     │   │
│   │                                                                     │   │
│   │   1. `feature/xxx-2025-12-18` - 说明                                │   │
│   │   2. `feature/yyy-2025-12-18` - 说明                                │   │
│   │   3. `docs/zzz-2025-12-18` - 说明                                   │   │
│   │                                                                     │   │
│   │   请选择一个，或告诉我您想要的名称。                                 │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                     │
│       ▼  [用户确认后]                                                       │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │ Step 3: 创建分支                                                     │   │
│   ├─────────────────────────────────────────────────────────────────────┤   │
│   │ • git checkout -b [branch-name]                                      │   │
│   │ • (可选) git add → commit → push                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**分支命名格式**:

```
type/description-YYYY-MM-DD
```

| Type | 说明 | 示例 |
|------|------|------|
| feature | 新功能 | `feature/user-auth-2025-12-18` |
| fix | Bug 修复 | `fix/login-error-2025-12-18` |
| refactor | 重构 | `refactor/api-structure-2025-12-18` |
| docs | 文档 | `docs/readme-update-2025-12-18` |
| chore | 杂项 | `chore/cleanup-2025-12-18` |

**⚠️ 关键规则**: 必须等用户确认分支名后再创建，不要自动执行!

---

### 5.5 Commands vs Agents 对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│              Slash Commands vs Sub-Agents 选择指南                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────┐   ┌─────────────────────────────┐        │
│   │     📝 Slash Commands       │   │      🤖 Sub-Agents          │        │
│   ├─────────────────────────────┤   ├─────────────────────────────┤        │
│   │ 触发: 用户主动输入 /xxx     │   │ 触发: Claude 自动/按需委派  │        │
│   │ 位置: .claude/commands/     │   │ 位置: .claude/agents/       │        │
│   │ 格式: 纯 Markdown 指令      │   │ 格式: YAML frontmatter + MD │        │
│   │ Context: 在主对话中执行     │   │ Context: 独立 context window│        │
│   │ 适用: 标准化工作流          │   │ 适用: 专业化任务委派        │        │
│   └─────────────────────────────┘   └─────────────────────────────┘        │
│                                                                             │
│   💡 选择建议:                                                               │
│   • 固定步骤的工作流 (git commit, PR) → Slash Command                       │
│   • 需要 Claude 判断何时调用 → Sub-Agent                                    │
│   • 需要上下文隔离 → Sub-Agent                                              │
│   • 需要限制工具权限 → Sub-Agent                                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Transferable Rules (可迁移规则)

### 规则 #1: Context 隔离优先于功能堆砌

**模式**:
```
❌ 错误模式: 一个 Agent 做所有事 → context 爆炸 → 质量下降
✅ 正确模式: 主 Agent 做决策 + Sub-Agents 做执行 → context 干净 → 长会话
```

**为什么有效**: LLM 的 context window 是有限资源。探索性搜索产生大量"垃圾"内容，如果都在主对话中进行，很快就会达到 context 限制或质量下降。

**如何应用**: 任何需要大量搜索、调试、或试错的任务，都应该委派给 Sub-Agent。

---

### 规则 #2: Description 是自动路由的关键

**模式**:
```
❌ 错误模式: description 写得模糊 → Claude 不知道何时调用
✅ 正确模式: description 包含具体触发条件 → 自动且准确的调用
```

**为什么有效**: Claude 根据用户请求和 Agent 的 `description` 字段决定是否委派任务。

**如何应用**: 写 description 时问自己——"用户说什么话时，我希望这个 Agent 被调用？"

---

### 规则 #3: 工具权限最小化原则

**模式**:
```
❌ 错误模式: 所有 Agent 都能访问所有工具
✅ 正确模式: 只给 Agent 完成任务所需的最小工具集
```

**为什么有效**:
1. 安全性 — 限制潜在风险
2. 聚焦性 — Agent 不会被不相关的工具分心
3. 可预测性 — 输出更一致

**如何应用**: Explore 类任务只需 Read/Grep/Glob；修改类任务才给 Edit。

---

### 规则 #4: 用 `inherit` 保持模型一致性

**模式**:
```
❌ 错误模式: 随意指定模型 → 风格/能力不一致
✅ 正确模式: 用 model: inherit → 与主对话保持一致
```

**为什么有效**: 如果主对话用 Opus，Sub-Agent 却用 Haiku，可能出现能力断层或风格差异。

**如何应用**: 除非有明确的成本/速度考虑，否则用 `inherit`。

---

### 规则 #5: 版本控制你的 Agent

**模式**:
```
❌ 错误模式: Agent 文件只在本地 → 团队无法共享/迭代
✅ 正确模式: .claude/agents/ 提交到 git → 团队协作优化
```

**为什么有效**: Agent 的 system prompt 是团队知识的积累，应该像代码一样被版本控制。

**如何应用**: 项目级 Agent 放 `.claude/agents/`，加入 git 仓库。

---

## Key Takeaways

### For Individual Engineers
1. **用 Explore Agent 做搜索** — 结果不污染主对话，保持 context 干净
2. **创建个人工具箱** — 在 `~/.claude/agents/` 放常用的 Agent (code-reviewer, debugger)
3. **学会 Resume** — 大型任务分多次完成，用 `agentId` 恢复上下文

### For Teams / Managers
1. **标准化团队 Agents** — 项目级 Agent 提交到 git，确保一致性
2. **建立 Agent 库** — 收集最佳实践的 Agent 配置，新人可快速复用
3. **Review Agent Prompts** — 像 review 代码一样 review Agent 的 system prompt

### For Founders / Leaders
1. **Sub-Agent 是 AI 原生架构** — 不是"多个 AI"，而是"任务分层 + 上下文管理"
2. **投资 Agent 设计** — 好的 Agent 配置是可复用的团队资产
3. **关注 context 效率** — 这是 LLM 应用中最被低估的瓶颈

---

## Glossary (术语表)

| 英文术语 | 中文翻译 | 说明 |
|----------|----------|------|
| Sub-Agent | 子代理 | 被主 Agent 委派任务的专业化 Agent |
| Context Window | 上下文窗口 | LLM 单次对话可处理的最大 token 数 |
| Context Preservation | 上下文保护 | Sub-Agent 的结果不进入主对话 context |
| Tool Sandboxing | 工具沙箱 | 限制 Agent 可访问的工具集 |
| Resumable Agent | 可恢复 Agent | 可以用 agentId 继续之前对话的 Agent |
| Thoroughness Level | 彻底程度 | Explore Agent 的搜索深度 (quick/medium/very thorough) |
| Permission Mode | 权限模式 | Agent 处理权限请求的方式 |

---

## Sources

- [Claude Code Sub-Agents Documentation](https://code.claude.com/docs/en/sub-agents)
- [Claude Code Tools Documentation](https://code.claude.com/docs/en/tools)
- [Claude Code Plugins Documentation](https://code.claude.com/docs/en/plugins)

---

**文档版本**: v1.1.0
**创建日期**: 2025-12-18
**最后更新**: 2025-12-20
**文档类型**: KNOW 知识提取文档

### 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0.0 | 2025-12-18 | 初始版本：Sub-Agents 核心文档 |
| v1.1.0 | 2025-12-20 | 新增第五部分：自定义 Slash Commands (/commit, /pr, /branch) |
